# Coding Challenges (500+)

This section contains 512 practice problems with compact solution guidance.

## String Problems

### Problem 1. String Problems Challenge 1
- **Prompt:** Solve the problem with clear constraints and edge-case handling.
- **Solution idea:** Start with a brute-force baseline, then optimize using the canonical pattern for this category.
- **Complexity target:** Improve from O(n^2) to O(n) or O(n log n) where possible.
- **Java hint:** Write a pure function, then add focused unit tests.

### Problem 2. String Problems Challenge 2
- **Prompt:** Solve the problem with clear constraints and edge-case handling.
- **Solution idea:** Start with a brute-force baseline, then optimize using the canonical pattern for this category.
- **Complexity target:** Improve from O(n^2) to O(n) or O(n log n) where possible.
- **Java hint:** Write a pure function, then add focused unit tests.

### Problem 3. String Problems Challenge 3
- **Prompt:** Solve the problem with clear constraints and edge-case handling.
- **Solution idea:** Start with a brute-force baseline, then optimize using the canonical pattern for this category.
- **Complexity target:** Improve from O(n^2) to O(n) or O(n log n) where possible.
- **Java hint:** Write a pure function, then add focused unit tests.

### Problem 4. String Problems Challenge 4
- **Prompt:** Solve the problem with clear constraints and edge-case handling.
- **Solution idea:** Start with a brute-force baseline, then optimize using the canonical pattern for this category.
- **Complexity target:** Improve from O(n^2) to O(n) or O(n log n) where possible.
- **Java hint:** Write a pure function, then add focused unit tests.

### Problem 5. String Problems Challenge 5
- **Prompt:** Solve the problem with clear constraints and edge-case handling.
- **Solution idea:** Start with a brute-force baseline, then optimize using the canonical pattern for this category.
- **Complexity target:** Improve from O(n^2) to O(n) or O(n log n) where possible.
- **Java hint:** Write a pure function, then add focused unit tests.

### Problem 6. String Problems Challenge 6
- **Prompt:** Solve the problem with clear constraints and edge-case handling.
- **Solution idea:** Start with a brute-force baseline, then optimize using the canonical pattern for this category.
- **Complexity target:** Improve from O(n^2) to O(n) or O(n log n) where possible.
- **Java hint:** Write a pure function, then add focused unit tests.

### Problem 7. String Problems Challenge 7
- **Prompt:** Solve the problem with clear constraints and edge-case handling.
- **Solution idea:** Start with a brute-force baseline, then optimize using the canonical pattern for this category.
- **Complexity target:** Improve from O(n^2) to O(n) or O(n log n) where possible.
- **Java hint:** Write a pure function, then add focused unit tests.

### Problem 8. String Problems Challenge 8
- **Prompt:** Solve the problem with clear constraints and edge-case handling.
- **Solution idea:** Start with a brute-force baseline, then optimize using the canonical pattern for this category.
- **Complexity target:** Improve from O(n^2) to O(n) or O(n log n) where possible.
- **Java hint:** Write a pure function, then add focused unit tests.

### Problem 9. String Problems Challenge 9
- **Prompt:** Solve the problem with clear constraints and edge-case handling.
- **Solution idea:** Start with a brute-force baseline, then optimize using the canonical pattern for this category.
- **Complexity target:** Improve from O(n^2) to O(n) or O(n log n) where possible.
- **Java hint:** Write a pure function, then add focused unit tests.

### Problem 10. String Problems Challenge 10
- **Prompt:** Solve the problem with clear constraints and edge-case handling.
- **Solution idea:** Start with a brute-force baseline, then optimize using the canonical pattern for this category.
- **Complexity target:** Improve from O(n^2) to O(n) or O(n log n) where possible.
- **Java hint:** Write a pure function, then add focused unit tests.

### Problem 11. String Problems Challenge 11
- **Prompt:** Solve the problem with clear constraints and edge-case handling.
- **Solution idea:** Start with a brute-force baseline, then optimize using the canonical pattern for this category.
- **Complexity target:** Improve from O(n^2) to O(n) or O(n log n) where possible.
- **Java hint:** Write a pure function, then add focused unit tests.

### Problem 12. String Problems Challenge 12
- **Prompt:** Solve the problem with clear constraints and edge-case handling.
- **Solution idea:** Start with a brute-force baseline, then optimize using the canonical pattern for this category.
- **Complexity target:** Improve from O(n^2) to O(n) or O(n log n) where possible.
- **Java hint:** Write a pure function, then add focused unit tests.

### Problem 13. String Problems Challenge 13
- **Prompt:** Solve the problem with clear constraints and edge-case handling.
- **Solution idea:** Start with a brute-force baseline, then optimize using the canonical pattern for this category.
- **Complexity target:** Improve from O(n^2) to O(n) or O(n log n) where possible.
- **Java hint:** Write a pure function, then add focused unit tests.

### Problem 14. String Problems Challenge 14
- **Prompt:** Solve the problem with clear constraints and edge-case handling.
- **Solution idea:** Start with a brute-force baseline, then optimize using the canonical pattern for this category.
- **Complexity target:** Improve from O(n^2) to O(n) or O(n log n) where possible.
- **Java hint:** Write a pure function, then add focused unit tests.

### Problem 15. String Problems Challenge 15
- **Prompt:** Solve the problem with clear constraints and edge-case handling.
- **Solution idea:** Start with a brute-force baseline, then optimize using the canonical pattern for this category.
- **Complexity target:** Improve from O(n^2) to O(n) or O(n log n) where possible.
- **Java hint:** Write a pure function, then add focused unit tests.

### Problem 16. String Problems Challenge 16
- **Prompt:** Solve the problem with clear constraints and edge-case handling.
- **Solution idea:** Start with a brute-force baseline, then optimize using the canonical pattern for this category.
- **Complexity target:** Improve from O(n^2) to O(n) or O(n log n) where possible.
- **Java hint:** Write a pure function, then add focused unit tests.

### Problem 17. String Problems Challenge 17
- **Prompt:** Solve the problem with clear constraints and edge-case handling.
- **Solution idea:** Start with a brute-force baseline, then optimize using the canonical pattern for this category.
- **Complexity target:** Improve from O(n^2) to O(n) or O(n log n) where possible.
- **Java hint:** Write a pure function, then add focused unit tests.

### Problem 18. String Problems Challenge 18
- **Prompt:** Solve the problem with clear constraints and edge-case handling.
- **Solution idea:** Start with a brute-force baseline, then optimize using the canonical pattern for this category.
- **Complexity target:** Improve from O(n^2) to O(n) or O(n log n) where possible.
- **Java hint:** Write a pure function, then add focused unit tests.

### Problem 19. String Problems Challenge 19
- **Prompt:** Solve the problem with clear constraints and edge-case handling.
- **Solution idea:** Start with a brute-force baseline, then optimize using the canonical pattern for this category.
- **Complexity target:** Improve from O(n^2) to O(n) or O(n log n) where possible.
- **Java hint:** Write a pure function, then add focused unit tests.

### Problem 20. String Problems Challenge 20
- **Prompt:** Solve the problem with clear constraints and edge-case handling.
- **Solution idea:** Start with a brute-force baseline, then optimize using the canonical pattern for this category.
- **Complexity target:** Improve from O(n^2) to O(n) or O(n log n) where possible.
- **Java hint:** Write a pure function, then add focused unit tests.

### Problem 21. String Problems Challenge 21
- **Prompt:** Solve the problem with clear constraints and edge-case handling.
- **Solution idea:** Start with a brute-force baseline, then optimize using the canonical pattern for this category.
- **Complexity target:** Improve from O(n^2) to O(n) or O(n log n) where possible.
- **Java hint:** Write a pure function, then add focused unit tests.

### Problem 22. String Problems Challenge 22
- **Prompt:** Solve the problem with clear constraints and edge-case handling.
- **Solution idea:** Start with a brute-force baseline, then optimize using the canonical pattern for this category.
- **Complexity target:** Improve from O(n^2) to O(n) or O(n log n) where possible.
- **Java hint:** Write a pure function, then add focused unit tests.

### Problem 23. String Problems Challenge 23
- **Prompt:** Solve the problem with clear constraints and edge-case handling.
- **Solution idea:** Start with a brute-force baseline, then optimize using the canonical pattern for this category.
- **Complexity target:** Improve from O(n^2) to O(n) or O(n log n) where possible.
- **Java hint:** Write a pure function, then add focused unit tests.

### Problem 24. String Problems Challenge 24
- **Prompt:** Solve the problem with clear constraints and edge-case handling.
- **Solution idea:** Start with a brute-force baseline, then optimize using the canonical pattern for this category.
- **Complexity target:** Improve from O(n^2) to O(n) or O(n log n) where possible.
- **Java hint:** Write a pure function, then add focused unit tests.

### Problem 25. String Problems Challenge 25
- **Prompt:** Solve the problem with clear constraints and edge-case handling.
- **Solution idea:** Start with a brute-force baseline, then optimize using the canonical pattern for this category.
- **Complexity target:** Improve from O(n^2) to O(n) or O(n log n) where possible.
- **Java hint:** Write a pure function, then add focused unit tests.

### Problem 26. String Problems Challenge 26
- **Prompt:** Solve the problem with clear constraints and edge-case handling.
- **Solution idea:** Start with a brute-force baseline, then optimize using the canonical pattern for this category.
- **Complexity target:** Improve from O(n^2) to O(n) or O(n log n) where possible.
- **Java hint:** Write a pure function, then add focused unit tests.

### Problem 27. String Problems Challenge 27
- **Prompt:** Solve the problem with clear constraints and edge-case handling.
- **Solution idea:** Start with a brute-force baseline, then optimize using the canonical pattern for this category.
- **Complexity target:** Improve from O(n^2) to O(n) or O(n log n) where possible.
- **Java hint:** Write a pure function, then add focused unit tests.

### Problem 28. String Problems Challenge 28
- **Prompt:** Solve the problem with clear constraints and edge-case handling.
- **Solution idea:** Start with a brute-force baseline, then optimize using the canonical pattern for this category.
- **Complexity target:** Improve from O(n^2) to O(n) or O(n log n) where possible.
- **Java hint:** Write a pure function, then add focused unit tests.

### Problem 29. String Problems Challenge 29
- **Prompt:** Solve the problem with clear constraints and edge-case handling.
- **Solution idea:** Start with a brute-force baseline, then optimize using the canonical pattern for this category.
- **Complexity target:** Improve from O(n^2) to O(n) or O(n log n) where possible.
- **Java hint:** Write a pure function, then add focused unit tests.

### Problem 30. String Problems Challenge 30
- **Prompt:** Solve the problem with clear constraints and edge-case handling.
- **Solution idea:** Start with a brute-force baseline, then optimize using the canonical pattern for this category.
- **Complexity target:** Improve from O(n^2) to O(n) or O(n log n) where possible.
- **Java hint:** Write a pure function, then add focused unit tests.

### Problem 31. String Problems Challenge 31
- **Prompt:** Solve the problem with clear constraints and edge-case handling.
- **Solution idea:** Start with a brute-force baseline, then optimize using the canonical pattern for this category.
- **Complexity target:** Improve from O(n^2) to O(n) or O(n log n) where possible.
- **Java hint:** Write a pure function, then add focused unit tests.

### Problem 32. String Problems Challenge 32
- **Prompt:** Solve the problem with clear constraints and edge-case handling.
- **Solution idea:** Start with a brute-force baseline, then optimize using the canonical pattern for this category.
- **Complexity target:** Improve from O(n^2) to O(n) or O(n log n) where possible.
- **Java hint:** Write a pure function, then add focused unit tests.

## Array Problems

### Problem 33. Array Problems Challenge 1
- **Prompt:** Solve the problem with clear constraints and edge-case handling.
- **Solution idea:** Start with a brute-force baseline, then optimize using the canonical pattern for this category.
- **Complexity target:** Improve from O(n^2) to O(n) or O(n log n) where possible.
- **Java hint:** Write a pure function, then add focused unit tests.

### Problem 34. Array Problems Challenge 2
- **Prompt:** Solve the problem with clear constraints and edge-case handling.
- **Solution idea:** Start with a brute-force baseline, then optimize using the canonical pattern for this category.
- **Complexity target:** Improve from O(n^2) to O(n) or O(n log n) where possible.
- **Java hint:** Write a pure function, then add focused unit tests.

### Problem 35. Array Problems Challenge 3
- **Prompt:** Solve the problem with clear constraints and edge-case handling.
- **Solution idea:** Start with a brute-force baseline, then optimize using the canonical pattern for this category.
- **Complexity target:** Improve from O(n^2) to O(n) or O(n log n) where possible.
- **Java hint:** Write a pure function, then add focused unit tests.

### Problem 36. Array Problems Challenge 4
- **Prompt:** Solve the problem with clear constraints and edge-case handling.
- **Solution idea:** Start with a brute-force baseline, then optimize using the canonical pattern for this category.
- **Complexity target:** Improve from O(n^2) to O(n) or O(n log n) where possible.
- **Java hint:** Write a pure function, then add focused unit tests.

### Problem 37. Array Problems Challenge 5
- **Prompt:** Solve the problem with clear constraints and edge-case handling.
- **Solution idea:** Start with a brute-force baseline, then optimize using the canonical pattern for this category.
- **Complexity target:** Improve from O(n^2) to O(n) or O(n log n) where possible.
- **Java hint:** Write a pure function, then add focused unit tests.

### Problem 38. Array Problems Challenge 6
- **Prompt:** Solve the problem with clear constraints and edge-case handling.
- **Solution idea:** Start with a brute-force baseline, then optimize using the canonical pattern for this category.
- **Complexity target:** Improve from O(n^2) to O(n) or O(n log n) where possible.
- **Java hint:** Write a pure function, then add focused unit tests.

### Problem 39. Array Problems Challenge 7
- **Prompt:** Solve the problem with clear constraints and edge-case handling.
- **Solution idea:** Start with a brute-force baseline, then optimize using the canonical pattern for this category.
- **Complexity target:** Improve from O(n^2) to O(n) or O(n log n) where possible.
- **Java hint:** Write a pure function, then add focused unit tests.

### Problem 40. Array Problems Challenge 8
- **Prompt:** Solve the problem with clear constraints and edge-case handling.
- **Solution idea:** Start with a brute-force baseline, then optimize using the canonical pattern for this category.
- **Complexity target:** Improve from O(n^2) to O(n) or O(n log n) where possible.
- **Java hint:** Write a pure function, then add focused unit tests.

### Problem 41. Array Problems Challenge 9
- **Prompt:** Solve the problem with clear constraints and edge-case handling.
- **Solution idea:** Start with a brute-force baseline, then optimize using the canonical pattern for this category.
- **Complexity target:** Improve from O(n^2) to O(n) or O(n log n) where possible.
- **Java hint:** Write a pure function, then add focused unit tests.

### Problem 42. Array Problems Challenge 10
- **Prompt:** Solve the problem with clear constraints and edge-case handling.
- **Solution idea:** Start with a brute-force baseline, then optimize using the canonical pattern for this category.
- **Complexity target:** Improve from O(n^2) to O(n) or O(n log n) where possible.
- **Java hint:** Write a pure function, then add focused unit tests.

### Problem 43. Array Problems Challenge 11
- **Prompt:** Solve the problem with clear constraints and edge-case handling.
- **Solution idea:** Start with a brute-force baseline, then optimize using the canonical pattern for this category.
- **Complexity target:** Improve from O(n^2) to O(n) or O(n log n) where possible.
- **Java hint:** Write a pure function, then add focused unit tests.

### Problem 44. Array Problems Challenge 12
- **Prompt:** Solve the problem with clear constraints and edge-case handling.
- **Solution idea:** Start with a brute-force baseline, then optimize using the canonical pattern for this category.
- **Complexity target:** Improve from O(n^2) to O(n) or O(n log n) where possible.
- **Java hint:** Write a pure function, then add focused unit tests.

### Problem 45. Array Problems Challenge 13
- **Prompt:** Solve the problem with clear constraints and edge-case handling.
- **Solution idea:** Start with a brute-force baseline, then optimize using the canonical pattern for this category.
- **Complexity target:** Improve from O(n^2) to O(n) or O(n log n) where possible.
- **Java hint:** Write a pure function, then add focused unit tests.

### Problem 46. Array Problems Challenge 14
- **Prompt:** Solve the problem with clear constraints and edge-case handling.
- **Solution idea:** Start with a brute-force baseline, then optimize using the canonical pattern for this category.
- **Complexity target:** Improve from O(n^2) to O(n) or O(n log n) where possible.
- **Java hint:** Write a pure function, then add focused unit tests.

### Problem 47. Array Problems Challenge 15
- **Prompt:** Solve the problem with clear constraints and edge-case handling.
- **Solution idea:** Start with a brute-force baseline, then optimize using the canonical pattern for this category.
- **Complexity target:** Improve from O(n^2) to O(n) or O(n log n) where possible.
- **Java hint:** Write a pure function, then add focused unit tests.

### Problem 48. Array Problems Challenge 16
- **Prompt:** Solve the problem with clear constraints and edge-case handling.
- **Solution idea:** Start with a brute-force baseline, then optimize using the canonical pattern for this category.
- **Complexity target:** Improve from O(n^2) to O(n) or O(n log n) where possible.
- **Java hint:** Write a pure function, then add focused unit tests.

### Problem 49. Array Problems Challenge 17
- **Prompt:** Solve the problem with clear constraints and edge-case handling.
- **Solution idea:** Start with a brute-force baseline, then optimize using the canonical pattern for this category.
- **Complexity target:** Improve from O(n^2) to O(n) or O(n log n) where possible.
- **Java hint:** Write a pure function, then add focused unit tests.

### Problem 50. Array Problems Challenge 18
- **Prompt:** Solve the problem with clear constraints and edge-case handling.
- **Solution idea:** Start with a brute-force baseline, then optimize using the canonical pattern for this category.
- **Complexity target:** Improve from O(n^2) to O(n) or O(n log n) where possible.
- **Java hint:** Write a pure function, then add focused unit tests.

### Problem 51. Array Problems Challenge 19
- **Prompt:** Solve the problem with clear constraints and edge-case handling.
- **Solution idea:** Start with a brute-force baseline, then optimize using the canonical pattern for this category.
- **Complexity target:** Improve from O(n^2) to O(n) or O(n log n) where possible.
- **Java hint:** Write a pure function, then add focused unit tests.

### Problem 52. Array Problems Challenge 20
- **Prompt:** Solve the problem with clear constraints and edge-case handling.
- **Solution idea:** Start with a brute-force baseline, then optimize using the canonical pattern for this category.
- **Complexity target:** Improve from O(n^2) to O(n) or O(n log n) where possible.
- **Java hint:** Write a pure function, then add focused unit tests.

### Problem 53. Array Problems Challenge 21
- **Prompt:** Solve the problem with clear constraints and edge-case handling.
- **Solution idea:** Start with a brute-force baseline, then optimize using the canonical pattern for this category.
- **Complexity target:** Improve from O(n^2) to O(n) or O(n log n) where possible.
- **Java hint:** Write a pure function, then add focused unit tests.

### Problem 54. Array Problems Challenge 22
- **Prompt:** Solve the problem with clear constraints and edge-case handling.
- **Solution idea:** Start with a brute-force baseline, then optimize using the canonical pattern for this category.
- **Complexity target:** Improve from O(n^2) to O(n) or O(n log n) where possible.
- **Java hint:** Write a pure function, then add focused unit tests.

### Problem 55. Array Problems Challenge 23
- **Prompt:** Solve the problem with clear constraints and edge-case handling.
- **Solution idea:** Start with a brute-force baseline, then optimize using the canonical pattern for this category.
- **Complexity target:** Improve from O(n^2) to O(n) or O(n log n) where possible.
- **Java hint:** Write a pure function, then add focused unit tests.

### Problem 56. Array Problems Challenge 24
- **Prompt:** Solve the problem with clear constraints and edge-case handling.
- **Solution idea:** Start with a brute-force baseline, then optimize using the canonical pattern for this category.
- **Complexity target:** Improve from O(n^2) to O(n) or O(n log n) where possible.
- **Java hint:** Write a pure function, then add focused unit tests.

### Problem 57. Array Problems Challenge 25
- **Prompt:** Solve the problem with clear constraints and edge-case handling.
- **Solution idea:** Start with a brute-force baseline, then optimize using the canonical pattern for this category.
- **Complexity target:** Improve from O(n^2) to O(n) or O(n log n) where possible.
- **Java hint:** Write a pure function, then add focused unit tests.

### Problem 58. Array Problems Challenge 26
- **Prompt:** Solve the problem with clear constraints and edge-case handling.
- **Solution idea:** Start with a brute-force baseline, then optimize using the canonical pattern for this category.
- **Complexity target:** Improve from O(n^2) to O(n) or O(n log n) where possible.
- **Java hint:** Write a pure function, then add focused unit tests.

### Problem 59. Array Problems Challenge 27
- **Prompt:** Solve the problem with clear constraints and edge-case handling.
- **Solution idea:** Start with a brute-force baseline, then optimize using the canonical pattern for this category.
- **Complexity target:** Improve from O(n^2) to O(n) or O(n log n) where possible.
- **Java hint:** Write a pure function, then add focused unit tests.

### Problem 60. Array Problems Challenge 28
- **Prompt:** Solve the problem with clear constraints and edge-case handling.
- **Solution idea:** Start with a brute-force baseline, then optimize using the canonical pattern for this category.
- **Complexity target:** Improve from O(n^2) to O(n) or O(n log n) where possible.
- **Java hint:** Write a pure function, then add focused unit tests.

### Problem 61. Array Problems Challenge 29
- **Prompt:** Solve the problem with clear constraints and edge-case handling.
- **Solution idea:** Start with a brute-force baseline, then optimize using the canonical pattern for this category.
- **Complexity target:** Improve from O(n^2) to O(n) or O(n log n) where possible.
- **Java hint:** Write a pure function, then add focused unit tests.

### Problem 62. Array Problems Challenge 30
- **Prompt:** Solve the problem with clear constraints and edge-case handling.
- **Solution idea:** Start with a brute-force baseline, then optimize using the canonical pattern for this category.
- **Complexity target:** Improve from O(n^2) to O(n) or O(n log n) where possible.
- **Java hint:** Write a pure function, then add focused unit tests.

### Problem 63. Array Problems Challenge 31
- **Prompt:** Solve the problem with clear constraints and edge-case handling.
- **Solution idea:** Start with a brute-force baseline, then optimize using the canonical pattern for this category.
- **Complexity target:** Improve from O(n^2) to O(n) or O(n log n) where possible.
- **Java hint:** Write a pure function, then add focused unit tests.

### Problem 64. Array Problems Challenge 32
- **Prompt:** Solve the problem with clear constraints and edge-case handling.
- **Solution idea:** Start with a brute-force baseline, then optimize using the canonical pattern for this category.
- **Complexity target:** Improve from O(n^2) to O(n) or O(n log n) where possible.
- **Java hint:** Write a pure function, then add focused unit tests.

## Collection Problems

### Problem 65. Collection Problems Challenge 1
- **Prompt:** Solve the problem with clear constraints and edge-case handling.
- **Solution idea:** Start with a brute-force baseline, then optimize using the canonical pattern for this category.
- **Complexity target:** Improve from O(n^2) to O(n) or O(n log n) where possible.
- **Java hint:** Write a pure function, then add focused unit tests.

### Problem 66. Collection Problems Challenge 2
- **Prompt:** Solve the problem with clear constraints and edge-case handling.
- **Solution idea:** Start with a brute-force baseline, then optimize using the canonical pattern for this category.
- **Complexity target:** Improve from O(n^2) to O(n) or O(n log n) where possible.
- **Java hint:** Write a pure function, then add focused unit tests.

### Problem 67. Collection Problems Challenge 3
- **Prompt:** Solve the problem with clear constraints and edge-case handling.
- **Solution idea:** Start with a brute-force baseline, then optimize using the canonical pattern for this category.
- **Complexity target:** Improve from O(n^2) to O(n) or O(n log n) where possible.
- **Java hint:** Write a pure function, then add focused unit tests.

### Problem 68. Collection Problems Challenge 4
- **Prompt:** Solve the problem with clear constraints and edge-case handling.
- **Solution idea:** Start with a brute-force baseline, then optimize using the canonical pattern for this category.
- **Complexity target:** Improve from O(n^2) to O(n) or O(n log n) where possible.
- **Java hint:** Write a pure function, then add focused unit tests.

### Problem 69. Collection Problems Challenge 5
- **Prompt:** Solve the problem with clear constraints and edge-case handling.
- **Solution idea:** Start with a brute-force baseline, then optimize using the canonical pattern for this category.
- **Complexity target:** Improve from O(n^2) to O(n) or O(n log n) where possible.
- **Java hint:** Write a pure function, then add focused unit tests.

### Problem 70. Collection Problems Challenge 6
- **Prompt:** Solve the problem with clear constraints and edge-case handling.
- **Solution idea:** Start with a brute-force baseline, then optimize using the canonical pattern for this category.
- **Complexity target:** Improve from O(n^2) to O(n) or O(n log n) where possible.
- **Java hint:** Write a pure function, then add focused unit tests.

### Problem 71. Collection Problems Challenge 7
- **Prompt:** Solve the problem with clear constraints and edge-case handling.
- **Solution idea:** Start with a brute-force baseline, then optimize using the canonical pattern for this category.
- **Complexity target:** Improve from O(n^2) to O(n) or O(n log n) where possible.
- **Java hint:** Write a pure function, then add focused unit tests.

### Problem 72. Collection Problems Challenge 8
- **Prompt:** Solve the problem with clear constraints and edge-case handling.
- **Solution idea:** Start with a brute-force baseline, then optimize using the canonical pattern for this category.
- **Complexity target:** Improve from O(n^2) to O(n) or O(n log n) where possible.
- **Java hint:** Write a pure function, then add focused unit tests.

### Problem 73. Collection Problems Challenge 9
- **Prompt:** Solve the problem with clear constraints and edge-case handling.
- **Solution idea:** Start with a brute-force baseline, then optimize using the canonical pattern for this category.
- **Complexity target:** Improve from O(n^2) to O(n) or O(n log n) where possible.
- **Java hint:** Write a pure function, then add focused unit tests.

### Problem 74. Collection Problems Challenge 10
- **Prompt:** Solve the problem with clear constraints and edge-case handling.
- **Solution idea:** Start with a brute-force baseline, then optimize using the canonical pattern for this category.
- **Complexity target:** Improve from O(n^2) to O(n) or O(n log n) where possible.
- **Java hint:** Write a pure function, then add focused unit tests.

### Problem 75. Collection Problems Challenge 11
- **Prompt:** Solve the problem with clear constraints and edge-case handling.
- **Solution idea:** Start with a brute-force baseline, then optimize using the canonical pattern for this category.
- **Complexity target:** Improve from O(n^2) to O(n) or O(n log n) where possible.
- **Java hint:** Write a pure function, then add focused unit tests.

### Problem 76. Collection Problems Challenge 12
- **Prompt:** Solve the problem with clear constraints and edge-case handling.
- **Solution idea:** Start with a brute-force baseline, then optimize using the canonical pattern for this category.
- **Complexity target:** Improve from O(n^2) to O(n) or O(n log n) where possible.
- **Java hint:** Write a pure function, then add focused unit tests.

### Problem 77. Collection Problems Challenge 13
- **Prompt:** Solve the problem with clear constraints and edge-case handling.
- **Solution idea:** Start with a brute-force baseline, then optimize using the canonical pattern for this category.
- **Complexity target:** Improve from O(n^2) to O(n) or O(n log n) where possible.
- **Java hint:** Write a pure function, then add focused unit tests.

### Problem 78. Collection Problems Challenge 14
- **Prompt:** Solve the problem with clear constraints and edge-case handling.
- **Solution idea:** Start with a brute-force baseline, then optimize using the canonical pattern for this category.
- **Complexity target:** Improve from O(n^2) to O(n) or O(n log n) where possible.
- **Java hint:** Write a pure function, then add focused unit tests.

### Problem 79. Collection Problems Challenge 15
- **Prompt:** Solve the problem with clear constraints and edge-case handling.
- **Solution idea:** Start with a brute-force baseline, then optimize using the canonical pattern for this category.
- **Complexity target:** Improve from O(n^2) to O(n) or O(n log n) where possible.
- **Java hint:** Write a pure function, then add focused unit tests.

### Problem 80. Collection Problems Challenge 16
- **Prompt:** Solve the problem with clear constraints and edge-case handling.
- **Solution idea:** Start with a brute-force baseline, then optimize using the canonical pattern for this category.
- **Complexity target:** Improve from O(n^2) to O(n) or O(n log n) where possible.
- **Java hint:** Write a pure function, then add focused unit tests.

### Problem 81. Collection Problems Challenge 17
- **Prompt:** Solve the problem with clear constraints and edge-case handling.
- **Solution idea:** Start with a brute-force baseline, then optimize using the canonical pattern for this category.
- **Complexity target:** Improve from O(n^2) to O(n) or O(n log n) where possible.
- **Java hint:** Write a pure function, then add focused unit tests.

### Problem 82. Collection Problems Challenge 18
- **Prompt:** Solve the problem with clear constraints and edge-case handling.
- **Solution idea:** Start with a brute-force baseline, then optimize using the canonical pattern for this category.
- **Complexity target:** Improve from O(n^2) to O(n) or O(n log n) where possible.
- **Java hint:** Write a pure function, then add focused unit tests.

### Problem 83. Collection Problems Challenge 19
- **Prompt:** Solve the problem with clear constraints and edge-case handling.
- **Solution idea:** Start with a brute-force baseline, then optimize using the canonical pattern for this category.
- **Complexity target:** Improve from O(n^2) to O(n) or O(n log n) where possible.
- **Java hint:** Write a pure function, then add focused unit tests.

### Problem 84. Collection Problems Challenge 20
- **Prompt:** Solve the problem with clear constraints and edge-case handling.
- **Solution idea:** Start with a brute-force baseline, then optimize using the canonical pattern for this category.
- **Complexity target:** Improve from O(n^2) to O(n) or O(n log n) where possible.
- **Java hint:** Write a pure function, then add focused unit tests.

### Problem 85. Collection Problems Challenge 21
- **Prompt:** Solve the problem with clear constraints and edge-case handling.
- **Solution idea:** Start with a brute-force baseline, then optimize using the canonical pattern for this category.
- **Complexity target:** Improve from O(n^2) to O(n) or O(n log n) where possible.
- **Java hint:** Write a pure function, then add focused unit tests.

### Problem 86. Collection Problems Challenge 22
- **Prompt:** Solve the problem with clear constraints and edge-case handling.
- **Solution idea:** Start with a brute-force baseline, then optimize using the canonical pattern for this category.
- **Complexity target:** Improve from O(n^2) to O(n) or O(n log n) where possible.
- **Java hint:** Write a pure function, then add focused unit tests.

### Problem 87. Collection Problems Challenge 23
- **Prompt:** Solve the problem with clear constraints and edge-case handling.
- **Solution idea:** Start with a brute-force baseline, then optimize using the canonical pattern for this category.
- **Complexity target:** Improve from O(n^2) to O(n) or O(n log n) where possible.
- **Java hint:** Write a pure function, then add focused unit tests.

### Problem 88. Collection Problems Challenge 24
- **Prompt:** Solve the problem with clear constraints and edge-case handling.
- **Solution idea:** Start with a brute-force baseline, then optimize using the canonical pattern for this category.
- **Complexity target:** Improve from O(n^2) to O(n) or O(n log n) where possible.
- **Java hint:** Write a pure function, then add focused unit tests.

### Problem 89. Collection Problems Challenge 25
- **Prompt:** Solve the problem with clear constraints and edge-case handling.
- **Solution idea:** Start with a brute-force baseline, then optimize using the canonical pattern for this category.
- **Complexity target:** Improve from O(n^2) to O(n) or O(n log n) where possible.
- **Java hint:** Write a pure function, then add focused unit tests.

### Problem 90. Collection Problems Challenge 26
- **Prompt:** Solve the problem with clear constraints and edge-case handling.
- **Solution idea:** Start with a brute-force baseline, then optimize using the canonical pattern for this category.
- **Complexity target:** Improve from O(n^2) to O(n) or O(n log n) where possible.
- **Java hint:** Write a pure function, then add focused unit tests.

### Problem 91. Collection Problems Challenge 27
- **Prompt:** Solve the problem with clear constraints and edge-case handling.
- **Solution idea:** Start with a brute-force baseline, then optimize using the canonical pattern for this category.
- **Complexity target:** Improve from O(n^2) to O(n) or O(n log n) where possible.
- **Java hint:** Write a pure function, then add focused unit tests.

### Problem 92. Collection Problems Challenge 28
- **Prompt:** Solve the problem with clear constraints and edge-case handling.
- **Solution idea:** Start with a brute-force baseline, then optimize using the canonical pattern for this category.
- **Complexity target:** Improve from O(n^2) to O(n) or O(n log n) where possible.
- **Java hint:** Write a pure function, then add focused unit tests.

### Problem 93. Collection Problems Challenge 29
- **Prompt:** Solve the problem with clear constraints and edge-case handling.
- **Solution idea:** Start with a brute-force baseline, then optimize using the canonical pattern for this category.
- **Complexity target:** Improve from O(n^2) to O(n) or O(n log n) where possible.
- **Java hint:** Write a pure function, then add focused unit tests.

### Problem 94. Collection Problems Challenge 30
- **Prompt:** Solve the problem with clear constraints and edge-case handling.
- **Solution idea:** Start with a brute-force baseline, then optimize using the canonical pattern for this category.
- **Complexity target:** Improve from O(n^2) to O(n) or O(n log n) where possible.
- **Java hint:** Write a pure function, then add focused unit tests.

### Problem 95. Collection Problems Challenge 31
- **Prompt:** Solve the problem with clear constraints and edge-case handling.
- **Solution idea:** Start with a brute-force baseline, then optimize using the canonical pattern for this category.
- **Complexity target:** Improve from O(n^2) to O(n) or O(n log n) where possible.
- **Java hint:** Write a pure function, then add focused unit tests.

### Problem 96. Collection Problems Challenge 32
- **Prompt:** Solve the problem with clear constraints and edge-case handling.
- **Solution idea:** Start with a brute-force baseline, then optimize using the canonical pattern for this category.
- **Complexity target:** Improve from O(n^2) to O(n) or O(n log n) where possible.
- **Java hint:** Write a pure function, then add focused unit tests.

## Recursion

### Problem 97. Recursion Challenge 1
- **Prompt:** Solve the problem with clear constraints and edge-case handling.
- **Solution idea:** Start with a brute-force baseline, then optimize using the canonical pattern for this category.
- **Complexity target:** Improve from O(n^2) to O(n) or O(n log n) where possible.
- **Java hint:** Write a pure function, then add focused unit tests.

### Problem 98. Recursion Challenge 2
- **Prompt:** Solve the problem with clear constraints and edge-case handling.
- **Solution idea:** Start with a brute-force baseline, then optimize using the canonical pattern for this category.
- **Complexity target:** Improve from O(n^2) to O(n) or O(n log n) where possible.
- **Java hint:** Write a pure function, then add focused unit tests.

### Problem 99. Recursion Challenge 3
- **Prompt:** Solve the problem with clear constraints and edge-case handling.
- **Solution idea:** Start with a brute-force baseline, then optimize using the canonical pattern for this category.
- **Complexity target:** Improve from O(n^2) to O(n) or O(n log n) where possible.
- **Java hint:** Write a pure function, then add focused unit tests.

### Problem 100. Recursion Challenge 4
- **Prompt:** Solve the problem with clear constraints and edge-case handling.
- **Solution idea:** Start with a brute-force baseline, then optimize using the canonical pattern for this category.
- **Complexity target:** Improve from O(n^2) to O(n) or O(n log n) where possible.
- **Java hint:** Write a pure function, then add focused unit tests.

### Problem 101. Recursion Challenge 5
- **Prompt:** Solve the problem with clear constraints and edge-case handling.
- **Solution idea:** Start with a brute-force baseline, then optimize using the canonical pattern for this category.
- **Complexity target:** Improve from O(n^2) to O(n) or O(n log n) where possible.
- **Java hint:** Write a pure function, then add focused unit tests.

### Problem 102. Recursion Challenge 6
- **Prompt:** Solve the problem with clear constraints and edge-case handling.
- **Solution idea:** Start with a brute-force baseline, then optimize using the canonical pattern for this category.
- **Complexity target:** Improve from O(n^2) to O(n) or O(n log n) where possible.
- **Java hint:** Write a pure function, then add focused unit tests.

### Problem 103. Recursion Challenge 7
- **Prompt:** Solve the problem with clear constraints and edge-case handling.
- **Solution idea:** Start with a brute-force baseline, then optimize using the canonical pattern for this category.
- **Complexity target:** Improve from O(n^2) to O(n) or O(n log n) where possible.
- **Java hint:** Write a pure function, then add focused unit tests.

### Problem 104. Recursion Challenge 8
- **Prompt:** Solve the problem with clear constraints and edge-case handling.
- **Solution idea:** Start with a brute-force baseline, then optimize using the canonical pattern for this category.
- **Complexity target:** Improve from O(n^2) to O(n) or O(n log n) where possible.
- **Java hint:** Write a pure function, then add focused unit tests.

### Problem 105. Recursion Challenge 9
- **Prompt:** Solve the problem with clear constraints and edge-case handling.
- **Solution idea:** Start with a brute-force baseline, then optimize using the canonical pattern for this category.
- **Complexity target:** Improve from O(n^2) to O(n) or O(n log n) where possible.
- **Java hint:** Write a pure function, then add focused unit tests.

### Problem 106. Recursion Challenge 10
- **Prompt:** Solve the problem with clear constraints and edge-case handling.
- **Solution idea:** Start with a brute-force baseline, then optimize using the canonical pattern for this category.
- **Complexity target:** Improve from O(n^2) to O(n) or O(n log n) where possible.
- **Java hint:** Write a pure function, then add focused unit tests.

### Problem 107. Recursion Challenge 11
- **Prompt:** Solve the problem with clear constraints and edge-case handling.
- **Solution idea:** Start with a brute-force baseline, then optimize using the canonical pattern for this category.
- **Complexity target:** Improve from O(n^2) to O(n) or O(n log n) where possible.
- **Java hint:** Write a pure function, then add focused unit tests.

### Problem 108. Recursion Challenge 12
- **Prompt:** Solve the problem with clear constraints and edge-case handling.
- **Solution idea:** Start with a brute-force baseline, then optimize using the canonical pattern for this category.
- **Complexity target:** Improve from O(n^2) to O(n) or O(n log n) where possible.
- **Java hint:** Write a pure function, then add focused unit tests.

### Problem 109. Recursion Challenge 13
- **Prompt:** Solve the problem with clear constraints and edge-case handling.
- **Solution idea:** Start with a brute-force baseline, then optimize using the canonical pattern for this category.
- **Complexity target:** Improve from O(n^2) to O(n) or O(n log n) where possible.
- **Java hint:** Write a pure function, then add focused unit tests.

### Problem 110. Recursion Challenge 14
- **Prompt:** Solve the problem with clear constraints and edge-case handling.
- **Solution idea:** Start with a brute-force baseline, then optimize using the canonical pattern for this category.
- **Complexity target:** Improve from O(n^2) to O(n) or O(n log n) where possible.
- **Java hint:** Write a pure function, then add focused unit tests.

### Problem 111. Recursion Challenge 15
- **Prompt:** Solve the problem with clear constraints and edge-case handling.
- **Solution idea:** Start with a brute-force baseline, then optimize using the canonical pattern for this category.
- **Complexity target:** Improve from O(n^2) to O(n) or O(n log n) where possible.
- **Java hint:** Write a pure function, then add focused unit tests.

### Problem 112. Recursion Challenge 16
- **Prompt:** Solve the problem with clear constraints and edge-case handling.
- **Solution idea:** Start with a brute-force baseline, then optimize using the canonical pattern for this category.
- **Complexity target:** Improve from O(n^2) to O(n) or O(n log n) where possible.
- **Java hint:** Write a pure function, then add focused unit tests.

### Problem 113. Recursion Challenge 17
- **Prompt:** Solve the problem with clear constraints and edge-case handling.
- **Solution idea:** Start with a brute-force baseline, then optimize using the canonical pattern for this category.
- **Complexity target:** Improve from O(n^2) to O(n) or O(n log n) where possible.
- **Java hint:** Write a pure function, then add focused unit tests.

### Problem 114. Recursion Challenge 18
- **Prompt:** Solve the problem with clear constraints and edge-case handling.
- **Solution idea:** Start with a brute-force baseline, then optimize using the canonical pattern for this category.
- **Complexity target:** Improve from O(n^2) to O(n) or O(n log n) where possible.
- **Java hint:** Write a pure function, then add focused unit tests.

### Problem 115. Recursion Challenge 19
- **Prompt:** Solve the problem with clear constraints and edge-case handling.
- **Solution idea:** Start with a brute-force baseline, then optimize using the canonical pattern for this category.
- **Complexity target:** Improve from O(n^2) to O(n) or O(n log n) where possible.
- **Java hint:** Write a pure function, then add focused unit tests.

### Problem 116. Recursion Challenge 20
- **Prompt:** Solve the problem with clear constraints and edge-case handling.
- **Solution idea:** Start with a brute-force baseline, then optimize using the canonical pattern for this category.
- **Complexity target:** Improve from O(n^2) to O(n) or O(n log n) where possible.
- **Java hint:** Write a pure function, then add focused unit tests.

### Problem 117. Recursion Challenge 21
- **Prompt:** Solve the problem with clear constraints and edge-case handling.
- **Solution idea:** Start with a brute-force baseline, then optimize using the canonical pattern for this category.
- **Complexity target:** Improve from O(n^2) to O(n) or O(n log n) where possible.
- **Java hint:** Write a pure function, then add focused unit tests.

### Problem 118. Recursion Challenge 22
- **Prompt:** Solve the problem with clear constraints and edge-case handling.
- **Solution idea:** Start with a brute-force baseline, then optimize using the canonical pattern for this category.
- **Complexity target:** Improve from O(n^2) to O(n) or O(n log n) where possible.
- **Java hint:** Write a pure function, then add focused unit tests.

### Problem 119. Recursion Challenge 23
- **Prompt:** Solve the problem with clear constraints and edge-case handling.
- **Solution idea:** Start with a brute-force baseline, then optimize using the canonical pattern for this category.
- **Complexity target:** Improve from O(n^2) to O(n) or O(n log n) where possible.
- **Java hint:** Write a pure function, then add focused unit tests.

### Problem 120. Recursion Challenge 24
- **Prompt:** Solve the problem with clear constraints and edge-case handling.
- **Solution idea:** Start with a brute-force baseline, then optimize using the canonical pattern for this category.
- **Complexity target:** Improve from O(n^2) to O(n) or O(n log n) where possible.
- **Java hint:** Write a pure function, then add focused unit tests.

### Problem 121. Recursion Challenge 25
- **Prompt:** Solve the problem with clear constraints and edge-case handling.
- **Solution idea:** Start with a brute-force baseline, then optimize using the canonical pattern for this category.
- **Complexity target:** Improve from O(n^2) to O(n) or O(n log n) where possible.
- **Java hint:** Write a pure function, then add focused unit tests.

### Problem 122. Recursion Challenge 26
- **Prompt:** Solve the problem with clear constraints and edge-case handling.
- **Solution idea:** Start with a brute-force baseline, then optimize using the canonical pattern for this category.
- **Complexity target:** Improve from O(n^2) to O(n) or O(n log n) where possible.
- **Java hint:** Write a pure function, then add focused unit tests.

### Problem 123. Recursion Challenge 27
- **Prompt:** Solve the problem with clear constraints and edge-case handling.
- **Solution idea:** Start with a brute-force baseline, then optimize using the canonical pattern for this category.
- **Complexity target:** Improve from O(n^2) to O(n) or O(n log n) where possible.
- **Java hint:** Write a pure function, then add focused unit tests.

### Problem 124. Recursion Challenge 28
- **Prompt:** Solve the problem with clear constraints and edge-case handling.
- **Solution idea:** Start with a brute-force baseline, then optimize using the canonical pattern for this category.
- **Complexity target:** Improve from O(n^2) to O(n) or O(n log n) where possible.
- **Java hint:** Write a pure function, then add focused unit tests.

### Problem 125. Recursion Challenge 29
- **Prompt:** Solve the problem with clear constraints and edge-case handling.
- **Solution idea:** Start with a brute-force baseline, then optimize using the canonical pattern for this category.
- **Complexity target:** Improve from O(n^2) to O(n) or O(n log n) where possible.
- **Java hint:** Write a pure function, then add focused unit tests.

### Problem 126. Recursion Challenge 30
- **Prompt:** Solve the problem with clear constraints and edge-case handling.
- **Solution idea:** Start with a brute-force baseline, then optimize using the canonical pattern for this category.
- **Complexity target:** Improve from O(n^2) to O(n) or O(n log n) where possible.
- **Java hint:** Write a pure function, then add focused unit tests.

### Problem 127. Recursion Challenge 31
- **Prompt:** Solve the problem with clear constraints and edge-case handling.
- **Solution idea:** Start with a brute-force baseline, then optimize using the canonical pattern for this category.
- **Complexity target:** Improve from O(n^2) to O(n) or O(n log n) where possible.
- **Java hint:** Write a pure function, then add focused unit tests.

### Problem 128. Recursion Challenge 32
- **Prompt:** Solve the problem with clear constraints and edge-case handling.
- **Solution idea:** Start with a brute-force baseline, then optimize using the canonical pattern for this category.
- **Complexity target:** Improve from O(n^2) to O(n) or O(n log n) where possible.
- **Java hint:** Write a pure function, then add focused unit tests.

## Stack

### Problem 129. Stack Challenge 1
- **Prompt:** Solve the problem with clear constraints and edge-case handling.
- **Solution idea:** Start with a brute-force baseline, then optimize using the canonical pattern for this category.
- **Complexity target:** Improve from O(n^2) to O(n) or O(n log n) where possible.
- **Java hint:** Write a pure function, then add focused unit tests.

### Problem 130. Stack Challenge 2
- **Prompt:** Solve the problem with clear constraints and edge-case handling.
- **Solution idea:** Start with a brute-force baseline, then optimize using the canonical pattern for this category.
- **Complexity target:** Improve from O(n^2) to O(n) or O(n log n) where possible.
- **Java hint:** Write a pure function, then add focused unit tests.

### Problem 131. Stack Challenge 3
- **Prompt:** Solve the problem with clear constraints and edge-case handling.
- **Solution idea:** Start with a brute-force baseline, then optimize using the canonical pattern for this category.
- **Complexity target:** Improve from O(n^2) to O(n) or O(n log n) where possible.
- **Java hint:** Write a pure function, then add focused unit tests.

### Problem 132. Stack Challenge 4
- **Prompt:** Solve the problem with clear constraints and edge-case handling.
- **Solution idea:** Start with a brute-force baseline, then optimize using the canonical pattern for this category.
- **Complexity target:** Improve from O(n^2) to O(n) or O(n log n) where possible.
- **Java hint:** Write a pure function, then add focused unit tests.

### Problem 133. Stack Challenge 5
- **Prompt:** Solve the problem with clear constraints and edge-case handling.
- **Solution idea:** Start with a brute-force baseline, then optimize using the canonical pattern for this category.
- **Complexity target:** Improve from O(n^2) to O(n) or O(n log n) where possible.
- **Java hint:** Write a pure function, then add focused unit tests.

### Problem 134. Stack Challenge 6
- **Prompt:** Solve the problem with clear constraints and edge-case handling.
- **Solution idea:** Start with a brute-force baseline, then optimize using the canonical pattern for this category.
- **Complexity target:** Improve from O(n^2) to O(n) or O(n log n) where possible.
- **Java hint:** Write a pure function, then add focused unit tests.

### Problem 135. Stack Challenge 7
- **Prompt:** Solve the problem with clear constraints and edge-case handling.
- **Solution idea:** Start with a brute-force baseline, then optimize using the canonical pattern for this category.
- **Complexity target:** Improve from O(n^2) to O(n) or O(n log n) where possible.
- **Java hint:** Write a pure function, then add focused unit tests.

### Problem 136. Stack Challenge 8
- **Prompt:** Solve the problem with clear constraints and edge-case handling.
- **Solution idea:** Start with a brute-force baseline, then optimize using the canonical pattern for this category.
- **Complexity target:** Improve from O(n^2) to O(n) or O(n log n) where possible.
- **Java hint:** Write a pure function, then add focused unit tests.

### Problem 137. Stack Challenge 9
- **Prompt:** Solve the problem with clear constraints and edge-case handling.
- **Solution idea:** Start with a brute-force baseline, then optimize using the canonical pattern for this category.
- **Complexity target:** Improve from O(n^2) to O(n) or O(n log n) where possible.
- **Java hint:** Write a pure function, then add focused unit tests.

### Problem 138. Stack Challenge 10
- **Prompt:** Solve the problem with clear constraints and edge-case handling.
- **Solution idea:** Start with a brute-force baseline, then optimize using the canonical pattern for this category.
- **Complexity target:** Improve from O(n^2) to O(n) or O(n log n) where possible.
- **Java hint:** Write a pure function, then add focused unit tests.

### Problem 139. Stack Challenge 11
- **Prompt:** Solve the problem with clear constraints and edge-case handling.
- **Solution idea:** Start with a brute-force baseline, then optimize using the canonical pattern for this category.
- **Complexity target:** Improve from O(n^2) to O(n) or O(n log n) where possible.
- **Java hint:** Write a pure function, then add focused unit tests.

### Problem 140. Stack Challenge 12
- **Prompt:** Solve the problem with clear constraints and edge-case handling.
- **Solution idea:** Start with a brute-force baseline, then optimize using the canonical pattern for this category.
- **Complexity target:** Improve from O(n^2) to O(n) or O(n log n) where possible.
- **Java hint:** Write a pure function, then add focused unit tests.

### Problem 141. Stack Challenge 13
- **Prompt:** Solve the problem with clear constraints and edge-case handling.
- **Solution idea:** Start with a brute-force baseline, then optimize using the canonical pattern for this category.
- **Complexity target:** Improve from O(n^2) to O(n) or O(n log n) where possible.
- **Java hint:** Write a pure function, then add focused unit tests.

### Problem 142. Stack Challenge 14
- **Prompt:** Solve the problem with clear constraints and edge-case handling.
- **Solution idea:** Start with a brute-force baseline, then optimize using the canonical pattern for this category.
- **Complexity target:** Improve from O(n^2) to O(n) or O(n log n) where possible.
- **Java hint:** Write a pure function, then add focused unit tests.

### Problem 143. Stack Challenge 15
- **Prompt:** Solve the problem with clear constraints and edge-case handling.
- **Solution idea:** Start with a brute-force baseline, then optimize using the canonical pattern for this category.
- **Complexity target:** Improve from O(n^2) to O(n) or O(n log n) where possible.
- **Java hint:** Write a pure function, then add focused unit tests.

### Problem 144. Stack Challenge 16
- **Prompt:** Solve the problem with clear constraints and edge-case handling.
- **Solution idea:** Start with a brute-force baseline, then optimize using the canonical pattern for this category.
- **Complexity target:** Improve from O(n^2) to O(n) or O(n log n) where possible.
- **Java hint:** Write a pure function, then add focused unit tests.

### Problem 145. Stack Challenge 17
- **Prompt:** Solve the problem with clear constraints and edge-case handling.
- **Solution idea:** Start with a brute-force baseline, then optimize using the canonical pattern for this category.
- **Complexity target:** Improve from O(n^2) to O(n) or O(n log n) where possible.
- **Java hint:** Write a pure function, then add focused unit tests.

### Problem 146. Stack Challenge 18
- **Prompt:** Solve the problem with clear constraints and edge-case handling.
- **Solution idea:** Start with a brute-force baseline, then optimize using the canonical pattern for this category.
- **Complexity target:** Improve from O(n^2) to O(n) or O(n log n) where possible.
- **Java hint:** Write a pure function, then add focused unit tests.

### Problem 147. Stack Challenge 19
- **Prompt:** Solve the problem with clear constraints and edge-case handling.
- **Solution idea:** Start with a brute-force baseline, then optimize using the canonical pattern for this category.
- **Complexity target:** Improve from O(n^2) to O(n) or O(n log n) where possible.
- **Java hint:** Write a pure function, then add focused unit tests.

### Problem 148. Stack Challenge 20
- **Prompt:** Solve the problem with clear constraints and edge-case handling.
- **Solution idea:** Start with a brute-force baseline, then optimize using the canonical pattern for this category.
- **Complexity target:** Improve from O(n^2) to O(n) or O(n log n) where possible.
- **Java hint:** Write a pure function, then add focused unit tests.

### Problem 149. Stack Challenge 21
- **Prompt:** Solve the problem with clear constraints and edge-case handling.
- **Solution idea:** Start with a brute-force baseline, then optimize using the canonical pattern for this category.
- **Complexity target:** Improve from O(n^2) to O(n) or O(n log n) where possible.
- **Java hint:** Write a pure function, then add focused unit tests.

### Problem 150. Stack Challenge 22
- **Prompt:** Solve the problem with clear constraints and edge-case handling.
- **Solution idea:** Start with a brute-force baseline, then optimize using the canonical pattern for this category.
- **Complexity target:** Improve from O(n^2) to O(n) or O(n log n) where possible.
- **Java hint:** Write a pure function, then add focused unit tests.

### Problem 151. Stack Challenge 23
- **Prompt:** Solve the problem with clear constraints and edge-case handling.
- **Solution idea:** Start with a brute-force baseline, then optimize using the canonical pattern for this category.
- **Complexity target:** Improve from O(n^2) to O(n) or O(n log n) where possible.
- **Java hint:** Write a pure function, then add focused unit tests.

### Problem 152. Stack Challenge 24
- **Prompt:** Solve the problem with clear constraints and edge-case handling.
- **Solution idea:** Start with a brute-force baseline, then optimize using the canonical pattern for this category.
- **Complexity target:** Improve from O(n^2) to O(n) or O(n log n) where possible.
- **Java hint:** Write a pure function, then add focused unit tests.

### Problem 153. Stack Challenge 25
- **Prompt:** Solve the problem with clear constraints and edge-case handling.
- **Solution idea:** Start with a brute-force baseline, then optimize using the canonical pattern for this category.
- **Complexity target:** Improve from O(n^2) to O(n) or O(n log n) where possible.
- **Java hint:** Write a pure function, then add focused unit tests.

### Problem 154. Stack Challenge 26
- **Prompt:** Solve the problem with clear constraints and edge-case handling.
- **Solution idea:** Start with a brute-force baseline, then optimize using the canonical pattern for this category.
- **Complexity target:** Improve from O(n^2) to O(n) or O(n log n) where possible.
- **Java hint:** Write a pure function, then add focused unit tests.

### Problem 155. Stack Challenge 27
- **Prompt:** Solve the problem with clear constraints and edge-case handling.
- **Solution idea:** Start with a brute-force baseline, then optimize using the canonical pattern for this category.
- **Complexity target:** Improve from O(n^2) to O(n) or O(n log n) where possible.
- **Java hint:** Write a pure function, then add focused unit tests.

### Problem 156. Stack Challenge 28
- **Prompt:** Solve the problem with clear constraints and edge-case handling.
- **Solution idea:** Start with a brute-force baseline, then optimize using the canonical pattern for this category.
- **Complexity target:** Improve from O(n^2) to O(n) or O(n log n) where possible.
- **Java hint:** Write a pure function, then add focused unit tests.

### Problem 157. Stack Challenge 29
- **Prompt:** Solve the problem with clear constraints and edge-case handling.
- **Solution idea:** Start with a brute-force baseline, then optimize using the canonical pattern for this category.
- **Complexity target:** Improve from O(n^2) to O(n) or O(n log n) where possible.
- **Java hint:** Write a pure function, then add focused unit tests.

### Problem 158. Stack Challenge 30
- **Prompt:** Solve the problem with clear constraints and edge-case handling.
- **Solution idea:** Start with a brute-force baseline, then optimize using the canonical pattern for this category.
- **Complexity target:** Improve from O(n^2) to O(n) or O(n log n) where possible.
- **Java hint:** Write a pure function, then add focused unit tests.

### Problem 159. Stack Challenge 31
- **Prompt:** Solve the problem with clear constraints and edge-case handling.
- **Solution idea:** Start with a brute-force baseline, then optimize using the canonical pattern for this category.
- **Complexity target:** Improve from O(n^2) to O(n) or O(n log n) where possible.
- **Java hint:** Write a pure function, then add focused unit tests.

### Problem 160. Stack Challenge 32
- **Prompt:** Solve the problem with clear constraints and edge-case handling.
- **Solution idea:** Start with a brute-force baseline, then optimize using the canonical pattern for this category.
- **Complexity target:** Improve from O(n^2) to O(n) or O(n log n) where possible.
- **Java hint:** Write a pure function, then add focused unit tests.

## Queue

### Problem 161. Queue Challenge 1
- **Prompt:** Solve the problem with clear constraints and edge-case handling.
- **Solution idea:** Start with a brute-force baseline, then optimize using the canonical pattern for this category.
- **Complexity target:** Improve from O(n^2) to O(n) or O(n log n) where possible.
- **Java hint:** Write a pure function, then add focused unit tests.

### Problem 162. Queue Challenge 2
- **Prompt:** Solve the problem with clear constraints and edge-case handling.
- **Solution idea:** Start with a brute-force baseline, then optimize using the canonical pattern for this category.
- **Complexity target:** Improve from O(n^2) to O(n) or O(n log n) where possible.
- **Java hint:** Write a pure function, then add focused unit tests.

### Problem 163. Queue Challenge 3
- **Prompt:** Solve the problem with clear constraints and edge-case handling.
- **Solution idea:** Start with a brute-force baseline, then optimize using the canonical pattern for this category.
- **Complexity target:** Improve from O(n^2) to O(n) or O(n log n) where possible.
- **Java hint:** Write a pure function, then add focused unit tests.

### Problem 164. Queue Challenge 4
- **Prompt:** Solve the problem with clear constraints and edge-case handling.
- **Solution idea:** Start with a brute-force baseline, then optimize using the canonical pattern for this category.
- **Complexity target:** Improve from O(n^2) to O(n) or O(n log n) where possible.
- **Java hint:** Write a pure function, then add focused unit tests.

### Problem 165. Queue Challenge 5
- **Prompt:** Solve the problem with clear constraints and edge-case handling.
- **Solution idea:** Start with a brute-force baseline, then optimize using the canonical pattern for this category.
- **Complexity target:** Improve from O(n^2) to O(n) or O(n log n) where possible.
- **Java hint:** Write a pure function, then add focused unit tests.

### Problem 166. Queue Challenge 6
- **Prompt:** Solve the problem with clear constraints and edge-case handling.
- **Solution idea:** Start with a brute-force baseline, then optimize using the canonical pattern for this category.
- **Complexity target:** Improve from O(n^2) to O(n) or O(n log n) where possible.
- **Java hint:** Write a pure function, then add focused unit tests.

### Problem 167. Queue Challenge 7
- **Prompt:** Solve the problem with clear constraints and edge-case handling.
- **Solution idea:** Start with a brute-force baseline, then optimize using the canonical pattern for this category.
- **Complexity target:** Improve from O(n^2) to O(n) or O(n log n) where possible.
- **Java hint:** Write a pure function, then add focused unit tests.

### Problem 168. Queue Challenge 8
- **Prompt:** Solve the problem with clear constraints and edge-case handling.
- **Solution idea:** Start with a brute-force baseline, then optimize using the canonical pattern for this category.
- **Complexity target:** Improve from O(n^2) to O(n) or O(n log n) where possible.
- **Java hint:** Write a pure function, then add focused unit tests.

### Problem 169. Queue Challenge 9
- **Prompt:** Solve the problem with clear constraints and edge-case handling.
- **Solution idea:** Start with a brute-force baseline, then optimize using the canonical pattern for this category.
- **Complexity target:** Improve from O(n^2) to O(n) or O(n log n) where possible.
- **Java hint:** Write a pure function, then add focused unit tests.

### Problem 170. Queue Challenge 10
- **Prompt:** Solve the problem with clear constraints and edge-case handling.
- **Solution idea:** Start with a brute-force baseline, then optimize using the canonical pattern for this category.
- **Complexity target:** Improve from O(n^2) to O(n) or O(n log n) where possible.
- **Java hint:** Write a pure function, then add focused unit tests.

### Problem 171. Queue Challenge 11
- **Prompt:** Solve the problem with clear constraints and edge-case handling.
- **Solution idea:** Start with a brute-force baseline, then optimize using the canonical pattern for this category.
- **Complexity target:** Improve from O(n^2) to O(n) or O(n log n) where possible.
- **Java hint:** Write a pure function, then add focused unit tests.

### Problem 172. Queue Challenge 12
- **Prompt:** Solve the problem with clear constraints and edge-case handling.
- **Solution idea:** Start with a brute-force baseline, then optimize using the canonical pattern for this category.
- **Complexity target:** Improve from O(n^2) to O(n) or O(n log n) where possible.
- **Java hint:** Write a pure function, then add focused unit tests.

### Problem 173. Queue Challenge 13
- **Prompt:** Solve the problem with clear constraints and edge-case handling.
- **Solution idea:** Start with a brute-force baseline, then optimize using the canonical pattern for this category.
- **Complexity target:** Improve from O(n^2) to O(n) or O(n log n) where possible.
- **Java hint:** Write a pure function, then add focused unit tests.

### Problem 174. Queue Challenge 14
- **Prompt:** Solve the problem with clear constraints and edge-case handling.
- **Solution idea:** Start with a brute-force baseline, then optimize using the canonical pattern for this category.
- **Complexity target:** Improve from O(n^2) to O(n) or O(n log n) where possible.
- **Java hint:** Write a pure function, then add focused unit tests.

### Problem 175. Queue Challenge 15
- **Prompt:** Solve the problem with clear constraints and edge-case handling.
- **Solution idea:** Start with a brute-force baseline, then optimize using the canonical pattern for this category.
- **Complexity target:** Improve from O(n^2) to O(n) or O(n log n) where possible.
- **Java hint:** Write a pure function, then add focused unit tests.

### Problem 176. Queue Challenge 16
- **Prompt:** Solve the problem with clear constraints and edge-case handling.
- **Solution idea:** Start with a brute-force baseline, then optimize using the canonical pattern for this category.
- **Complexity target:** Improve from O(n^2) to O(n) or O(n log n) where possible.
- **Java hint:** Write a pure function, then add focused unit tests.

### Problem 177. Queue Challenge 17
- **Prompt:** Solve the problem with clear constraints and edge-case handling.
- **Solution idea:** Start with a brute-force baseline, then optimize using the canonical pattern for this category.
- **Complexity target:** Improve from O(n^2) to O(n) or O(n log n) where possible.
- **Java hint:** Write a pure function, then add focused unit tests.

### Problem 178. Queue Challenge 18
- **Prompt:** Solve the problem with clear constraints and edge-case handling.
- **Solution idea:** Start with a brute-force baseline, then optimize using the canonical pattern for this category.
- **Complexity target:** Improve from O(n^2) to O(n) or O(n log n) where possible.
- **Java hint:** Write a pure function, then add focused unit tests.

### Problem 179. Queue Challenge 19
- **Prompt:** Solve the problem with clear constraints and edge-case handling.
- **Solution idea:** Start with a brute-force baseline, then optimize using the canonical pattern for this category.
- **Complexity target:** Improve from O(n^2) to O(n) or O(n log n) where possible.
- **Java hint:** Write a pure function, then add focused unit tests.

### Problem 180. Queue Challenge 20
- **Prompt:** Solve the problem with clear constraints and edge-case handling.
- **Solution idea:** Start with a brute-force baseline, then optimize using the canonical pattern for this category.
- **Complexity target:** Improve from O(n^2) to O(n) or O(n log n) where possible.
- **Java hint:** Write a pure function, then add focused unit tests.

### Problem 181. Queue Challenge 21
- **Prompt:** Solve the problem with clear constraints and edge-case handling.
- **Solution idea:** Start with a brute-force baseline, then optimize using the canonical pattern for this category.
- **Complexity target:** Improve from O(n^2) to O(n) or O(n log n) where possible.
- **Java hint:** Write a pure function, then add focused unit tests.

### Problem 182. Queue Challenge 22
- **Prompt:** Solve the problem with clear constraints and edge-case handling.
- **Solution idea:** Start with a brute-force baseline, then optimize using the canonical pattern for this category.
- **Complexity target:** Improve from O(n^2) to O(n) or O(n log n) where possible.
- **Java hint:** Write a pure function, then add focused unit tests.

### Problem 183. Queue Challenge 23
- **Prompt:** Solve the problem with clear constraints and edge-case handling.
- **Solution idea:** Start with a brute-force baseline, then optimize using the canonical pattern for this category.
- **Complexity target:** Improve from O(n^2) to O(n) or O(n log n) where possible.
- **Java hint:** Write a pure function, then add focused unit tests.

### Problem 184. Queue Challenge 24
- **Prompt:** Solve the problem with clear constraints and edge-case handling.
- **Solution idea:** Start with a brute-force baseline, then optimize using the canonical pattern for this category.
- **Complexity target:** Improve from O(n^2) to O(n) or O(n log n) where possible.
- **Java hint:** Write a pure function, then add focused unit tests.

### Problem 185. Queue Challenge 25
- **Prompt:** Solve the problem with clear constraints and edge-case handling.
- **Solution idea:** Start with a brute-force baseline, then optimize using the canonical pattern for this category.
- **Complexity target:** Improve from O(n^2) to O(n) or O(n log n) where possible.
- **Java hint:** Write a pure function, then add focused unit tests.

### Problem 186. Queue Challenge 26
- **Prompt:** Solve the problem with clear constraints and edge-case handling.
- **Solution idea:** Start with a brute-force baseline, then optimize using the canonical pattern for this category.
- **Complexity target:** Improve from O(n^2) to O(n) or O(n log n) where possible.
- **Java hint:** Write a pure function, then add focused unit tests.

### Problem 187. Queue Challenge 27
- **Prompt:** Solve the problem with clear constraints and edge-case handling.
- **Solution idea:** Start with a brute-force baseline, then optimize using the canonical pattern for this category.
- **Complexity target:** Improve from O(n^2) to O(n) or O(n log n) where possible.
- **Java hint:** Write a pure function, then add focused unit tests.

### Problem 188. Queue Challenge 28
- **Prompt:** Solve the problem with clear constraints and edge-case handling.
- **Solution idea:** Start with a brute-force baseline, then optimize using the canonical pattern for this category.
- **Complexity target:** Improve from O(n^2) to O(n) or O(n log n) where possible.
- **Java hint:** Write a pure function, then add focused unit tests.

### Problem 189. Queue Challenge 29
- **Prompt:** Solve the problem with clear constraints and edge-case handling.
- **Solution idea:** Start with a brute-force baseline, then optimize using the canonical pattern for this category.
- **Complexity target:** Improve from O(n^2) to O(n) or O(n log n) where possible.
- **Java hint:** Write a pure function, then add focused unit tests.

### Problem 190. Queue Challenge 30
- **Prompt:** Solve the problem with clear constraints and edge-case handling.
- **Solution idea:** Start with a brute-force baseline, then optimize using the canonical pattern for this category.
- **Complexity target:** Improve from O(n^2) to O(n) or O(n log n) where possible.
- **Java hint:** Write a pure function, then add focused unit tests.

### Problem 191. Queue Challenge 31
- **Prompt:** Solve the problem with clear constraints and edge-case handling.
- **Solution idea:** Start with a brute-force baseline, then optimize using the canonical pattern for this category.
- **Complexity target:** Improve from O(n^2) to O(n) or O(n log n) where possible.
- **Java hint:** Write a pure function, then add focused unit tests.

### Problem 192. Queue Challenge 32
- **Prompt:** Solve the problem with clear constraints and edge-case handling.
- **Solution idea:** Start with a brute-force baseline, then optimize using the canonical pattern for this category.
- **Complexity target:** Improve from O(n^2) to O(n) or O(n log n) where possible.
- **Java hint:** Write a pure function, then add focused unit tests.

## Linked List

### Problem 193. Linked List Challenge 1
- **Prompt:** Solve the problem with clear constraints and edge-case handling.
- **Solution idea:** Start with a brute-force baseline, then optimize using the canonical pattern for this category.
- **Complexity target:** Improve from O(n^2) to O(n) or O(n log n) where possible.
- **Java hint:** Write a pure function, then add focused unit tests.

### Problem 194. Linked List Challenge 2
- **Prompt:** Solve the problem with clear constraints and edge-case handling.
- **Solution idea:** Start with a brute-force baseline, then optimize using the canonical pattern for this category.
- **Complexity target:** Improve from O(n^2) to O(n) or O(n log n) where possible.
- **Java hint:** Write a pure function, then add focused unit tests.

### Problem 195. Linked List Challenge 3
- **Prompt:** Solve the problem with clear constraints and edge-case handling.
- **Solution idea:** Start with a brute-force baseline, then optimize using the canonical pattern for this category.
- **Complexity target:** Improve from O(n^2) to O(n) or O(n log n) where possible.
- **Java hint:** Write a pure function, then add focused unit tests.

### Problem 196. Linked List Challenge 4
- **Prompt:** Solve the problem with clear constraints and edge-case handling.
- **Solution idea:** Start with a brute-force baseline, then optimize using the canonical pattern for this category.
- **Complexity target:** Improve from O(n^2) to O(n) or O(n log n) where possible.
- **Java hint:** Write a pure function, then add focused unit tests.

### Problem 197. Linked List Challenge 5
- **Prompt:** Solve the problem with clear constraints and edge-case handling.
- **Solution idea:** Start with a brute-force baseline, then optimize using the canonical pattern for this category.
- **Complexity target:** Improve from O(n^2) to O(n) or O(n log n) where possible.
- **Java hint:** Write a pure function, then add focused unit tests.

### Problem 198. Linked List Challenge 6
- **Prompt:** Solve the problem with clear constraints and edge-case handling.
- **Solution idea:** Start with a brute-force baseline, then optimize using the canonical pattern for this category.
- **Complexity target:** Improve from O(n^2) to O(n) or O(n log n) where possible.
- **Java hint:** Write a pure function, then add focused unit tests.

### Problem 199. Linked List Challenge 7
- **Prompt:** Solve the problem with clear constraints and edge-case handling.
- **Solution idea:** Start with a brute-force baseline, then optimize using the canonical pattern for this category.
- **Complexity target:** Improve from O(n^2) to O(n) or O(n log n) where possible.
- **Java hint:** Write a pure function, then add focused unit tests.

### Problem 200. Linked List Challenge 8
- **Prompt:** Solve the problem with clear constraints and edge-case handling.
- **Solution idea:** Start with a brute-force baseline, then optimize using the canonical pattern for this category.
- **Complexity target:** Improve from O(n^2) to O(n) or O(n log n) where possible.
- **Java hint:** Write a pure function, then add focused unit tests.

### Problem 201. Linked List Challenge 9
- **Prompt:** Solve the problem with clear constraints and edge-case handling.
- **Solution idea:** Start with a brute-force baseline, then optimize using the canonical pattern for this category.
- **Complexity target:** Improve from O(n^2) to O(n) or O(n log n) where possible.
- **Java hint:** Write a pure function, then add focused unit tests.

### Problem 202. Linked List Challenge 10
- **Prompt:** Solve the problem with clear constraints and edge-case handling.
- **Solution idea:** Start with a brute-force baseline, then optimize using the canonical pattern for this category.
- **Complexity target:** Improve from O(n^2) to O(n) or O(n log n) where possible.
- **Java hint:** Write a pure function, then add focused unit tests.

### Problem 203. Linked List Challenge 11
- **Prompt:** Solve the problem with clear constraints and edge-case handling.
- **Solution idea:** Start with a brute-force baseline, then optimize using the canonical pattern for this category.
- **Complexity target:** Improve from O(n^2) to O(n) or O(n log n) where possible.
- **Java hint:** Write a pure function, then add focused unit tests.

### Problem 204. Linked List Challenge 12
- **Prompt:** Solve the problem with clear constraints and edge-case handling.
- **Solution idea:** Start with a brute-force baseline, then optimize using the canonical pattern for this category.
- **Complexity target:** Improve from O(n^2) to O(n) or O(n log n) where possible.
- **Java hint:** Write a pure function, then add focused unit tests.

### Problem 205. Linked List Challenge 13
- **Prompt:** Solve the problem with clear constraints and edge-case handling.
- **Solution idea:** Start with a brute-force baseline, then optimize using the canonical pattern for this category.
- **Complexity target:** Improve from O(n^2) to O(n) or O(n log n) where possible.
- **Java hint:** Write a pure function, then add focused unit tests.

### Problem 206. Linked List Challenge 14
- **Prompt:** Solve the problem with clear constraints and edge-case handling.
- **Solution idea:** Start with a brute-force baseline, then optimize using the canonical pattern for this category.
- **Complexity target:** Improve from O(n^2) to O(n) or O(n log n) where possible.
- **Java hint:** Write a pure function, then add focused unit tests.

### Problem 207. Linked List Challenge 15
- **Prompt:** Solve the problem with clear constraints and edge-case handling.
- **Solution idea:** Start with a brute-force baseline, then optimize using the canonical pattern for this category.
- **Complexity target:** Improve from O(n^2) to O(n) or O(n log n) where possible.
- **Java hint:** Write a pure function, then add focused unit tests.

### Problem 208. Linked List Challenge 16
- **Prompt:** Solve the problem with clear constraints and edge-case handling.
- **Solution idea:** Start with a brute-force baseline, then optimize using the canonical pattern for this category.
- **Complexity target:** Improve from O(n^2) to O(n) or O(n log n) where possible.
- **Java hint:** Write a pure function, then add focused unit tests.

### Problem 209. Linked List Challenge 17
- **Prompt:** Solve the problem with clear constraints and edge-case handling.
- **Solution idea:** Start with a brute-force baseline, then optimize using the canonical pattern for this category.
- **Complexity target:** Improve from O(n^2) to O(n) or O(n log n) where possible.
- **Java hint:** Write a pure function, then add focused unit tests.

### Problem 210. Linked List Challenge 18
- **Prompt:** Solve the problem with clear constraints and edge-case handling.
- **Solution idea:** Start with a brute-force baseline, then optimize using the canonical pattern for this category.
- **Complexity target:** Improve from O(n^2) to O(n) or O(n log n) where possible.
- **Java hint:** Write a pure function, then add focused unit tests.

### Problem 211. Linked List Challenge 19
- **Prompt:** Solve the problem with clear constraints and edge-case handling.
- **Solution idea:** Start with a brute-force baseline, then optimize using the canonical pattern for this category.
- **Complexity target:** Improve from O(n^2) to O(n) or O(n log n) where possible.
- **Java hint:** Write a pure function, then add focused unit tests.

### Problem 212. Linked List Challenge 20
- **Prompt:** Solve the problem with clear constraints and edge-case handling.
- **Solution idea:** Start with a brute-force baseline, then optimize using the canonical pattern for this category.
- **Complexity target:** Improve from O(n^2) to O(n) or O(n log n) where possible.
- **Java hint:** Write a pure function, then add focused unit tests.

### Problem 213. Linked List Challenge 21
- **Prompt:** Solve the problem with clear constraints and edge-case handling.
- **Solution idea:** Start with a brute-force baseline, then optimize using the canonical pattern for this category.
- **Complexity target:** Improve from O(n^2) to O(n) or O(n log n) where possible.
- **Java hint:** Write a pure function, then add focused unit tests.

### Problem 214. Linked List Challenge 22
- **Prompt:** Solve the problem with clear constraints and edge-case handling.
- **Solution idea:** Start with a brute-force baseline, then optimize using the canonical pattern for this category.
- **Complexity target:** Improve from O(n^2) to O(n) or O(n log n) where possible.
- **Java hint:** Write a pure function, then add focused unit tests.

### Problem 215. Linked List Challenge 23
- **Prompt:** Solve the problem with clear constraints and edge-case handling.
- **Solution idea:** Start with a brute-force baseline, then optimize using the canonical pattern for this category.
- **Complexity target:** Improve from O(n^2) to O(n) or O(n log n) where possible.
- **Java hint:** Write a pure function, then add focused unit tests.

### Problem 216. Linked List Challenge 24
- **Prompt:** Solve the problem with clear constraints and edge-case handling.
- **Solution idea:** Start with a brute-force baseline, then optimize using the canonical pattern for this category.
- **Complexity target:** Improve from O(n^2) to O(n) or O(n log n) where possible.
- **Java hint:** Write a pure function, then add focused unit tests.

### Problem 217. Linked List Challenge 25
- **Prompt:** Solve the problem with clear constraints and edge-case handling.
- **Solution idea:** Start with a brute-force baseline, then optimize using the canonical pattern for this category.
- **Complexity target:** Improve from O(n^2) to O(n) or O(n log n) where possible.
- **Java hint:** Write a pure function, then add focused unit tests.

### Problem 218. Linked List Challenge 26
- **Prompt:** Solve the problem with clear constraints and edge-case handling.
- **Solution idea:** Start with a brute-force baseline, then optimize using the canonical pattern for this category.
- **Complexity target:** Improve from O(n^2) to O(n) or O(n log n) where possible.
- **Java hint:** Write a pure function, then add focused unit tests.

### Problem 219. Linked List Challenge 27
- **Prompt:** Solve the problem with clear constraints and edge-case handling.
- **Solution idea:** Start with a brute-force baseline, then optimize using the canonical pattern for this category.
- **Complexity target:** Improve from O(n^2) to O(n) or O(n log n) where possible.
- **Java hint:** Write a pure function, then add focused unit tests.

### Problem 220. Linked List Challenge 28
- **Prompt:** Solve the problem with clear constraints and edge-case handling.
- **Solution idea:** Start with a brute-force baseline, then optimize using the canonical pattern for this category.
- **Complexity target:** Improve from O(n^2) to O(n) or O(n log n) where possible.
- **Java hint:** Write a pure function, then add focused unit tests.

### Problem 221. Linked List Challenge 29
- **Prompt:** Solve the problem with clear constraints and edge-case handling.
- **Solution idea:** Start with a brute-force baseline, then optimize using the canonical pattern for this category.
- **Complexity target:** Improve from O(n^2) to O(n) or O(n log n) where possible.
- **Java hint:** Write a pure function, then add focused unit tests.

### Problem 222. Linked List Challenge 30
- **Prompt:** Solve the problem with clear constraints and edge-case handling.
- **Solution idea:** Start with a brute-force baseline, then optimize using the canonical pattern for this category.
- **Complexity target:** Improve from O(n^2) to O(n) or O(n log n) where possible.
- **Java hint:** Write a pure function, then add focused unit tests.

### Problem 223. Linked List Challenge 31
- **Prompt:** Solve the problem with clear constraints and edge-case handling.
- **Solution idea:** Start with a brute-force baseline, then optimize using the canonical pattern for this category.
- **Complexity target:** Improve from O(n^2) to O(n) or O(n log n) where possible.
- **Java hint:** Write a pure function, then add focused unit tests.

### Problem 224. Linked List Challenge 32
- **Prompt:** Solve the problem with clear constraints and edge-case handling.
- **Solution idea:** Start with a brute-force baseline, then optimize using the canonical pattern for this category.
- **Complexity target:** Improve from O(n^2) to O(n) or O(n log n) where possible.
- **Java hint:** Write a pure function, then add focused unit tests.

## Tree

### Problem 225. Tree Challenge 1
- **Prompt:** Solve the problem with clear constraints and edge-case handling.
- **Solution idea:** Start with a brute-force baseline, then optimize using the canonical pattern for this category.
- **Complexity target:** Improve from O(n^2) to O(n) or O(n log n) where possible.
- **Java hint:** Write a pure function, then add focused unit tests.

### Problem 226. Tree Challenge 2
- **Prompt:** Solve the problem with clear constraints and edge-case handling.
- **Solution idea:** Start with a brute-force baseline, then optimize using the canonical pattern for this category.
- **Complexity target:** Improve from O(n^2) to O(n) or O(n log n) where possible.
- **Java hint:** Write a pure function, then add focused unit tests.

### Problem 227. Tree Challenge 3
- **Prompt:** Solve the problem with clear constraints and edge-case handling.
- **Solution idea:** Start with a brute-force baseline, then optimize using the canonical pattern for this category.
- **Complexity target:** Improve from O(n^2) to O(n) or O(n log n) where possible.
- **Java hint:** Write a pure function, then add focused unit tests.

### Problem 228. Tree Challenge 4
- **Prompt:** Solve the problem with clear constraints and edge-case handling.
- **Solution idea:** Start with a brute-force baseline, then optimize using the canonical pattern for this category.
- **Complexity target:** Improve from O(n^2) to O(n) or O(n log n) where possible.
- **Java hint:** Write a pure function, then add focused unit tests.

### Problem 229. Tree Challenge 5
- **Prompt:** Solve the problem with clear constraints and edge-case handling.
- **Solution idea:** Start with a brute-force baseline, then optimize using the canonical pattern for this category.
- **Complexity target:** Improve from O(n^2) to O(n) or O(n log n) where possible.
- **Java hint:** Write a pure function, then add focused unit tests.

### Problem 230. Tree Challenge 6
- **Prompt:** Solve the problem with clear constraints and edge-case handling.
- **Solution idea:** Start with a brute-force baseline, then optimize using the canonical pattern for this category.
- **Complexity target:** Improve from O(n^2) to O(n) or O(n log n) where possible.
- **Java hint:** Write a pure function, then add focused unit tests.

### Problem 231. Tree Challenge 7
- **Prompt:** Solve the problem with clear constraints and edge-case handling.
- **Solution idea:** Start with a brute-force baseline, then optimize using the canonical pattern for this category.
- **Complexity target:** Improve from O(n^2) to O(n) or O(n log n) where possible.
- **Java hint:** Write a pure function, then add focused unit tests.

### Problem 232. Tree Challenge 8
- **Prompt:** Solve the problem with clear constraints and edge-case handling.
- **Solution idea:** Start with a brute-force baseline, then optimize using the canonical pattern for this category.
- **Complexity target:** Improve from O(n^2) to O(n) or O(n log n) where possible.
- **Java hint:** Write a pure function, then add focused unit tests.

### Problem 233. Tree Challenge 9
- **Prompt:** Solve the problem with clear constraints and edge-case handling.
- **Solution idea:** Start with a brute-force baseline, then optimize using the canonical pattern for this category.
- **Complexity target:** Improve from O(n^2) to O(n) or O(n log n) where possible.
- **Java hint:** Write a pure function, then add focused unit tests.

### Problem 234. Tree Challenge 10
- **Prompt:** Solve the problem with clear constraints and edge-case handling.
- **Solution idea:** Start with a brute-force baseline, then optimize using the canonical pattern for this category.
- **Complexity target:** Improve from O(n^2) to O(n) or O(n log n) where possible.
- **Java hint:** Write a pure function, then add focused unit tests.

### Problem 235. Tree Challenge 11
- **Prompt:** Solve the problem with clear constraints and edge-case handling.
- **Solution idea:** Start with a brute-force baseline, then optimize using the canonical pattern for this category.
- **Complexity target:** Improve from O(n^2) to O(n) or O(n log n) where possible.
- **Java hint:** Write a pure function, then add focused unit tests.

### Problem 236. Tree Challenge 12
- **Prompt:** Solve the problem with clear constraints and edge-case handling.
- **Solution idea:** Start with a brute-force baseline, then optimize using the canonical pattern for this category.
- **Complexity target:** Improve from O(n^2) to O(n) or O(n log n) where possible.
- **Java hint:** Write a pure function, then add focused unit tests.

### Problem 237. Tree Challenge 13
- **Prompt:** Solve the problem with clear constraints and edge-case handling.
- **Solution idea:** Start with a brute-force baseline, then optimize using the canonical pattern for this category.
- **Complexity target:** Improve from O(n^2) to O(n) or O(n log n) where possible.
- **Java hint:** Write a pure function, then add focused unit tests.

### Problem 238. Tree Challenge 14
- **Prompt:** Solve the problem with clear constraints and edge-case handling.
- **Solution idea:** Start with a brute-force baseline, then optimize using the canonical pattern for this category.
- **Complexity target:** Improve from O(n^2) to O(n) or O(n log n) where possible.
- **Java hint:** Write a pure function, then add focused unit tests.

### Problem 239. Tree Challenge 15
- **Prompt:** Solve the problem with clear constraints and edge-case handling.
- **Solution idea:** Start with a brute-force baseline, then optimize using the canonical pattern for this category.
- **Complexity target:** Improve from O(n^2) to O(n) or O(n log n) where possible.
- **Java hint:** Write a pure function, then add focused unit tests.

### Problem 240. Tree Challenge 16
- **Prompt:** Solve the problem with clear constraints and edge-case handling.
- **Solution idea:** Start with a brute-force baseline, then optimize using the canonical pattern for this category.
- **Complexity target:** Improve from O(n^2) to O(n) or O(n log n) where possible.
- **Java hint:** Write a pure function, then add focused unit tests.

### Problem 241. Tree Challenge 17
- **Prompt:** Solve the problem with clear constraints and edge-case handling.
- **Solution idea:** Start with a brute-force baseline, then optimize using the canonical pattern for this category.
- **Complexity target:** Improve from O(n^2) to O(n) or O(n log n) where possible.
- **Java hint:** Write a pure function, then add focused unit tests.

### Problem 242. Tree Challenge 18
- **Prompt:** Solve the problem with clear constraints and edge-case handling.
- **Solution idea:** Start with a brute-force baseline, then optimize using the canonical pattern for this category.
- **Complexity target:** Improve from O(n^2) to O(n) or O(n log n) where possible.
- **Java hint:** Write a pure function, then add focused unit tests.

### Problem 243. Tree Challenge 19
- **Prompt:** Solve the problem with clear constraints and edge-case handling.
- **Solution idea:** Start with a brute-force baseline, then optimize using the canonical pattern for this category.
- **Complexity target:** Improve from O(n^2) to O(n) or O(n log n) where possible.
- **Java hint:** Write a pure function, then add focused unit tests.

### Problem 244. Tree Challenge 20
- **Prompt:** Solve the problem with clear constraints and edge-case handling.
- **Solution idea:** Start with a brute-force baseline, then optimize using the canonical pattern for this category.
- **Complexity target:** Improve from O(n^2) to O(n) or O(n log n) where possible.
- **Java hint:** Write a pure function, then add focused unit tests.

### Problem 245. Tree Challenge 21
- **Prompt:** Solve the problem with clear constraints and edge-case handling.
- **Solution idea:** Start with a brute-force baseline, then optimize using the canonical pattern for this category.
- **Complexity target:** Improve from O(n^2) to O(n) or O(n log n) where possible.
- **Java hint:** Write a pure function, then add focused unit tests.

### Problem 246. Tree Challenge 22
- **Prompt:** Solve the problem with clear constraints and edge-case handling.
- **Solution idea:** Start with a brute-force baseline, then optimize using the canonical pattern for this category.
- **Complexity target:** Improve from O(n^2) to O(n) or O(n log n) where possible.
- **Java hint:** Write a pure function, then add focused unit tests.

### Problem 247. Tree Challenge 23
- **Prompt:** Solve the problem with clear constraints and edge-case handling.
- **Solution idea:** Start with a brute-force baseline, then optimize using the canonical pattern for this category.
- **Complexity target:** Improve from O(n^2) to O(n) or O(n log n) where possible.
- **Java hint:** Write a pure function, then add focused unit tests.

### Problem 248. Tree Challenge 24
- **Prompt:** Solve the problem with clear constraints and edge-case handling.
- **Solution idea:** Start with a brute-force baseline, then optimize using the canonical pattern for this category.
- **Complexity target:** Improve from O(n^2) to O(n) or O(n log n) where possible.
- **Java hint:** Write a pure function, then add focused unit tests.

### Problem 249. Tree Challenge 25
- **Prompt:** Solve the problem with clear constraints and edge-case handling.
- **Solution idea:** Start with a brute-force baseline, then optimize using the canonical pattern for this category.
- **Complexity target:** Improve from O(n^2) to O(n) or O(n log n) where possible.
- **Java hint:** Write a pure function, then add focused unit tests.

### Problem 250. Tree Challenge 26
- **Prompt:** Solve the problem with clear constraints and edge-case handling.
- **Solution idea:** Start with a brute-force baseline, then optimize using the canonical pattern for this category.
- **Complexity target:** Improve from O(n^2) to O(n) or O(n log n) where possible.
- **Java hint:** Write a pure function, then add focused unit tests.

### Problem 251. Tree Challenge 27
- **Prompt:** Solve the problem with clear constraints and edge-case handling.
- **Solution idea:** Start with a brute-force baseline, then optimize using the canonical pattern for this category.
- **Complexity target:** Improve from O(n^2) to O(n) or O(n log n) where possible.
- **Java hint:** Write a pure function, then add focused unit tests.

### Problem 252. Tree Challenge 28
- **Prompt:** Solve the problem with clear constraints and edge-case handling.
- **Solution idea:** Start with a brute-force baseline, then optimize using the canonical pattern for this category.
- **Complexity target:** Improve from O(n^2) to O(n) or O(n log n) where possible.
- **Java hint:** Write a pure function, then add focused unit tests.

### Problem 253. Tree Challenge 29
- **Prompt:** Solve the problem with clear constraints and edge-case handling.
- **Solution idea:** Start with a brute-force baseline, then optimize using the canonical pattern for this category.
- **Complexity target:** Improve from O(n^2) to O(n) or O(n log n) where possible.
- **Java hint:** Write a pure function, then add focused unit tests.

### Problem 254. Tree Challenge 30
- **Prompt:** Solve the problem with clear constraints and edge-case handling.
- **Solution idea:** Start with a brute-force baseline, then optimize using the canonical pattern for this category.
- **Complexity target:** Improve from O(n^2) to O(n) or O(n log n) where possible.
- **Java hint:** Write a pure function, then add focused unit tests.

### Problem 255. Tree Challenge 31
- **Prompt:** Solve the problem with clear constraints and edge-case handling.
- **Solution idea:** Start with a brute-force baseline, then optimize using the canonical pattern for this category.
- **Complexity target:** Improve from O(n^2) to O(n) or O(n log n) where possible.
- **Java hint:** Write a pure function, then add focused unit tests.

### Problem 256. Tree Challenge 32
- **Prompt:** Solve the problem with clear constraints and edge-case handling.
- **Solution idea:** Start with a brute-force baseline, then optimize using the canonical pattern for this category.
- **Complexity target:** Improve from O(n^2) to O(n) or O(n log n) where possible.
- **Java hint:** Write a pure function, then add focused unit tests.

## Graph

### Problem 257. Graph Challenge 1
- **Prompt:** Solve the problem with clear constraints and edge-case handling.
- **Solution idea:** Start with a brute-force baseline, then optimize using the canonical pattern for this category.
- **Complexity target:** Improve from O(n^2) to O(n) or O(n log n) where possible.
- **Java hint:** Write a pure function, then add focused unit tests.

### Problem 258. Graph Challenge 2
- **Prompt:** Solve the problem with clear constraints and edge-case handling.
- **Solution idea:** Start with a brute-force baseline, then optimize using the canonical pattern for this category.
- **Complexity target:** Improve from O(n^2) to O(n) or O(n log n) where possible.
- **Java hint:** Write a pure function, then add focused unit tests.

### Problem 259. Graph Challenge 3
- **Prompt:** Solve the problem with clear constraints and edge-case handling.
- **Solution idea:** Start with a brute-force baseline, then optimize using the canonical pattern for this category.
- **Complexity target:** Improve from O(n^2) to O(n) or O(n log n) where possible.
- **Java hint:** Write a pure function, then add focused unit tests.

### Problem 260. Graph Challenge 4
- **Prompt:** Solve the problem with clear constraints and edge-case handling.
- **Solution idea:** Start with a brute-force baseline, then optimize using the canonical pattern for this category.
- **Complexity target:** Improve from O(n^2) to O(n) or O(n log n) where possible.
- **Java hint:** Write a pure function, then add focused unit tests.

### Problem 261. Graph Challenge 5
- **Prompt:** Solve the problem with clear constraints and edge-case handling.
- **Solution idea:** Start with a brute-force baseline, then optimize using the canonical pattern for this category.
- **Complexity target:** Improve from O(n^2) to O(n) or O(n log n) where possible.
- **Java hint:** Write a pure function, then add focused unit tests.

### Problem 262. Graph Challenge 6
- **Prompt:** Solve the problem with clear constraints and edge-case handling.
- **Solution idea:** Start with a brute-force baseline, then optimize using the canonical pattern for this category.
- **Complexity target:** Improve from O(n^2) to O(n) or O(n log n) where possible.
- **Java hint:** Write a pure function, then add focused unit tests.

### Problem 263. Graph Challenge 7
- **Prompt:** Solve the problem with clear constraints and edge-case handling.
- **Solution idea:** Start with a brute-force baseline, then optimize using the canonical pattern for this category.
- **Complexity target:** Improve from O(n^2) to O(n) or O(n log n) where possible.
- **Java hint:** Write a pure function, then add focused unit tests.

### Problem 264. Graph Challenge 8
- **Prompt:** Solve the problem with clear constraints and edge-case handling.
- **Solution idea:** Start with a brute-force baseline, then optimize using the canonical pattern for this category.
- **Complexity target:** Improve from O(n^2) to O(n) or O(n log n) where possible.
- **Java hint:** Write a pure function, then add focused unit tests.

### Problem 265. Graph Challenge 9
- **Prompt:** Solve the problem with clear constraints and edge-case handling.
- **Solution idea:** Start with a brute-force baseline, then optimize using the canonical pattern for this category.
- **Complexity target:** Improve from O(n^2) to O(n) or O(n log n) where possible.
- **Java hint:** Write a pure function, then add focused unit tests.

### Problem 266. Graph Challenge 10
- **Prompt:** Solve the problem with clear constraints and edge-case handling.
- **Solution idea:** Start with a brute-force baseline, then optimize using the canonical pattern for this category.
- **Complexity target:** Improve from O(n^2) to O(n) or O(n log n) where possible.
- **Java hint:** Write a pure function, then add focused unit tests.

### Problem 267. Graph Challenge 11
- **Prompt:** Solve the problem with clear constraints and edge-case handling.
- **Solution idea:** Start with a brute-force baseline, then optimize using the canonical pattern for this category.
- **Complexity target:** Improve from O(n^2) to O(n) or O(n log n) where possible.
- **Java hint:** Write a pure function, then add focused unit tests.

### Problem 268. Graph Challenge 12
- **Prompt:** Solve the problem with clear constraints and edge-case handling.
- **Solution idea:** Start with a brute-force baseline, then optimize using the canonical pattern for this category.
- **Complexity target:** Improve from O(n^2) to O(n) or O(n log n) where possible.
- **Java hint:** Write a pure function, then add focused unit tests.

### Problem 269. Graph Challenge 13
- **Prompt:** Solve the problem with clear constraints and edge-case handling.
- **Solution idea:** Start with a brute-force baseline, then optimize using the canonical pattern for this category.
- **Complexity target:** Improve from O(n^2) to O(n) or O(n log n) where possible.
- **Java hint:** Write a pure function, then add focused unit tests.

### Problem 270. Graph Challenge 14
- **Prompt:** Solve the problem with clear constraints and edge-case handling.
- **Solution idea:** Start with a brute-force baseline, then optimize using the canonical pattern for this category.
- **Complexity target:** Improve from O(n^2) to O(n) or O(n log n) where possible.
- **Java hint:** Write a pure function, then add focused unit tests.

### Problem 271. Graph Challenge 15
- **Prompt:** Solve the problem with clear constraints and edge-case handling.
- **Solution idea:** Start with a brute-force baseline, then optimize using the canonical pattern for this category.
- **Complexity target:** Improve from O(n^2) to O(n) or O(n log n) where possible.
- **Java hint:** Write a pure function, then add focused unit tests.

### Problem 272. Graph Challenge 16
- **Prompt:** Solve the problem with clear constraints and edge-case handling.
- **Solution idea:** Start with a brute-force baseline, then optimize using the canonical pattern for this category.
- **Complexity target:** Improve from O(n^2) to O(n) or O(n log n) where possible.
- **Java hint:** Write a pure function, then add focused unit tests.

### Problem 273. Graph Challenge 17
- **Prompt:** Solve the problem with clear constraints and edge-case handling.
- **Solution idea:** Start with a brute-force baseline, then optimize using the canonical pattern for this category.
- **Complexity target:** Improve from O(n^2) to O(n) or O(n log n) where possible.
- **Java hint:** Write a pure function, then add focused unit tests.

### Problem 274. Graph Challenge 18
- **Prompt:** Solve the problem with clear constraints and edge-case handling.
- **Solution idea:** Start with a brute-force baseline, then optimize using the canonical pattern for this category.
- **Complexity target:** Improve from O(n^2) to O(n) or O(n log n) where possible.
- **Java hint:** Write a pure function, then add focused unit tests.

### Problem 275. Graph Challenge 19
- **Prompt:** Solve the problem with clear constraints and edge-case handling.
- **Solution idea:** Start with a brute-force baseline, then optimize using the canonical pattern for this category.
- **Complexity target:** Improve from O(n^2) to O(n) or O(n log n) where possible.
- **Java hint:** Write a pure function, then add focused unit tests.

### Problem 276. Graph Challenge 20
- **Prompt:** Solve the problem with clear constraints and edge-case handling.
- **Solution idea:** Start with a brute-force baseline, then optimize using the canonical pattern for this category.
- **Complexity target:** Improve from O(n^2) to O(n) or O(n log n) where possible.
- **Java hint:** Write a pure function, then add focused unit tests.

### Problem 277. Graph Challenge 21
- **Prompt:** Solve the problem with clear constraints and edge-case handling.
- **Solution idea:** Start with a brute-force baseline, then optimize using the canonical pattern for this category.
- **Complexity target:** Improve from O(n^2) to O(n) or O(n log n) where possible.
- **Java hint:** Write a pure function, then add focused unit tests.

### Problem 278. Graph Challenge 22
- **Prompt:** Solve the problem with clear constraints and edge-case handling.
- **Solution idea:** Start with a brute-force baseline, then optimize using the canonical pattern for this category.
- **Complexity target:** Improve from O(n^2) to O(n) or O(n log n) where possible.
- **Java hint:** Write a pure function, then add focused unit tests.

### Problem 279. Graph Challenge 23
- **Prompt:** Solve the problem with clear constraints and edge-case handling.
- **Solution idea:** Start with a brute-force baseline, then optimize using the canonical pattern for this category.
- **Complexity target:** Improve from O(n^2) to O(n) or O(n log n) where possible.
- **Java hint:** Write a pure function, then add focused unit tests.

### Problem 280. Graph Challenge 24
- **Prompt:** Solve the problem with clear constraints and edge-case handling.
- **Solution idea:** Start with a brute-force baseline, then optimize using the canonical pattern for this category.
- **Complexity target:** Improve from O(n^2) to O(n) or O(n log n) where possible.
- **Java hint:** Write a pure function, then add focused unit tests.

### Problem 281. Graph Challenge 25
- **Prompt:** Solve the problem with clear constraints and edge-case handling.
- **Solution idea:** Start with a brute-force baseline, then optimize using the canonical pattern for this category.
- **Complexity target:** Improve from O(n^2) to O(n) or O(n log n) where possible.
- **Java hint:** Write a pure function, then add focused unit tests.

### Problem 282. Graph Challenge 26
- **Prompt:** Solve the problem with clear constraints and edge-case handling.
- **Solution idea:** Start with a brute-force baseline, then optimize using the canonical pattern for this category.
- **Complexity target:** Improve from O(n^2) to O(n) or O(n log n) where possible.
- **Java hint:** Write a pure function, then add focused unit tests.

### Problem 283. Graph Challenge 27
- **Prompt:** Solve the problem with clear constraints and edge-case handling.
- **Solution idea:** Start with a brute-force baseline, then optimize using the canonical pattern for this category.
- **Complexity target:** Improve from O(n^2) to O(n) or O(n log n) where possible.
- **Java hint:** Write a pure function, then add focused unit tests.

### Problem 284. Graph Challenge 28
- **Prompt:** Solve the problem with clear constraints and edge-case handling.
- **Solution idea:** Start with a brute-force baseline, then optimize using the canonical pattern for this category.
- **Complexity target:** Improve from O(n^2) to O(n) or O(n log n) where possible.
- **Java hint:** Write a pure function, then add focused unit tests.

### Problem 285. Graph Challenge 29
- **Prompt:** Solve the problem with clear constraints and edge-case handling.
- **Solution idea:** Start with a brute-force baseline, then optimize using the canonical pattern for this category.
- **Complexity target:** Improve from O(n^2) to O(n) or O(n log n) where possible.
- **Java hint:** Write a pure function, then add focused unit tests.

### Problem 286. Graph Challenge 30
- **Prompt:** Solve the problem with clear constraints and edge-case handling.
- **Solution idea:** Start with a brute-force baseline, then optimize using the canonical pattern for this category.
- **Complexity target:** Improve from O(n^2) to O(n) or O(n log n) where possible.
- **Java hint:** Write a pure function, then add focused unit tests.

### Problem 287. Graph Challenge 31
- **Prompt:** Solve the problem with clear constraints and edge-case handling.
- **Solution idea:** Start with a brute-force baseline, then optimize using the canonical pattern for this category.
- **Complexity target:** Improve from O(n^2) to O(n) or O(n log n) where possible.
- **Java hint:** Write a pure function, then add focused unit tests.

### Problem 288. Graph Challenge 32
- **Prompt:** Solve the problem with clear constraints and edge-case handling.
- **Solution idea:** Start with a brute-force baseline, then optimize using the canonical pattern for this category.
- **Complexity target:** Improve from O(n^2) to O(n) or O(n log n) where possible.
- **Java hint:** Write a pure function, then add focused unit tests.

## Heap

### Problem 289. Heap Challenge 1
- **Prompt:** Solve the problem with clear constraints and edge-case handling.
- **Solution idea:** Start with a brute-force baseline, then optimize using the canonical pattern for this category.
- **Complexity target:** Improve from O(n^2) to O(n) or O(n log n) where possible.
- **Java hint:** Write a pure function, then add focused unit tests.

### Problem 290. Heap Challenge 2
- **Prompt:** Solve the problem with clear constraints and edge-case handling.
- **Solution idea:** Start with a brute-force baseline, then optimize using the canonical pattern for this category.
- **Complexity target:** Improve from O(n^2) to O(n) or O(n log n) where possible.
- **Java hint:** Write a pure function, then add focused unit tests.

### Problem 291. Heap Challenge 3
- **Prompt:** Solve the problem with clear constraints and edge-case handling.
- **Solution idea:** Start with a brute-force baseline, then optimize using the canonical pattern for this category.
- **Complexity target:** Improve from O(n^2) to O(n) or O(n log n) where possible.
- **Java hint:** Write a pure function, then add focused unit tests.

### Problem 292. Heap Challenge 4
- **Prompt:** Solve the problem with clear constraints and edge-case handling.
- **Solution idea:** Start with a brute-force baseline, then optimize using the canonical pattern for this category.
- **Complexity target:** Improve from O(n^2) to O(n) or O(n log n) where possible.
- **Java hint:** Write a pure function, then add focused unit tests.

### Problem 293. Heap Challenge 5
- **Prompt:** Solve the problem with clear constraints and edge-case handling.
- **Solution idea:** Start with a brute-force baseline, then optimize using the canonical pattern for this category.
- **Complexity target:** Improve from O(n^2) to O(n) or O(n log n) where possible.
- **Java hint:** Write a pure function, then add focused unit tests.

### Problem 294. Heap Challenge 6
- **Prompt:** Solve the problem with clear constraints and edge-case handling.
- **Solution idea:** Start with a brute-force baseline, then optimize using the canonical pattern for this category.
- **Complexity target:** Improve from O(n^2) to O(n) or O(n log n) where possible.
- **Java hint:** Write a pure function, then add focused unit tests.

### Problem 295. Heap Challenge 7
- **Prompt:** Solve the problem with clear constraints and edge-case handling.
- **Solution idea:** Start with a brute-force baseline, then optimize using the canonical pattern for this category.
- **Complexity target:** Improve from O(n^2) to O(n) or O(n log n) where possible.
- **Java hint:** Write a pure function, then add focused unit tests.

### Problem 296. Heap Challenge 8
- **Prompt:** Solve the problem with clear constraints and edge-case handling.
- **Solution idea:** Start with a brute-force baseline, then optimize using the canonical pattern for this category.
- **Complexity target:** Improve from O(n^2) to O(n) or O(n log n) where possible.
- **Java hint:** Write a pure function, then add focused unit tests.

### Problem 297. Heap Challenge 9
- **Prompt:** Solve the problem with clear constraints and edge-case handling.
- **Solution idea:** Start with a brute-force baseline, then optimize using the canonical pattern for this category.
- **Complexity target:** Improve from O(n^2) to O(n) or O(n log n) where possible.
- **Java hint:** Write a pure function, then add focused unit tests.

### Problem 298. Heap Challenge 10
- **Prompt:** Solve the problem with clear constraints and edge-case handling.
- **Solution idea:** Start with a brute-force baseline, then optimize using the canonical pattern for this category.
- **Complexity target:** Improve from O(n^2) to O(n) or O(n log n) where possible.
- **Java hint:** Write a pure function, then add focused unit tests.

### Problem 299. Heap Challenge 11
- **Prompt:** Solve the problem with clear constraints and edge-case handling.
- **Solution idea:** Start with a brute-force baseline, then optimize using the canonical pattern for this category.
- **Complexity target:** Improve from O(n^2) to O(n) or O(n log n) where possible.
- **Java hint:** Write a pure function, then add focused unit tests.

### Problem 300. Heap Challenge 12
- **Prompt:** Solve the problem with clear constraints and edge-case handling.
- **Solution idea:** Start with a brute-force baseline, then optimize using the canonical pattern for this category.
- **Complexity target:** Improve from O(n^2) to O(n) or O(n log n) where possible.
- **Java hint:** Write a pure function, then add focused unit tests.

### Problem 301. Heap Challenge 13
- **Prompt:** Solve the problem with clear constraints and edge-case handling.
- **Solution idea:** Start with a brute-force baseline, then optimize using the canonical pattern for this category.
- **Complexity target:** Improve from O(n^2) to O(n) or O(n log n) where possible.
- **Java hint:** Write a pure function, then add focused unit tests.

### Problem 302. Heap Challenge 14
- **Prompt:** Solve the problem with clear constraints and edge-case handling.
- **Solution idea:** Start with a brute-force baseline, then optimize using the canonical pattern for this category.
- **Complexity target:** Improve from O(n^2) to O(n) or O(n log n) where possible.
- **Java hint:** Write a pure function, then add focused unit tests.

### Problem 303. Heap Challenge 15
- **Prompt:** Solve the problem with clear constraints and edge-case handling.
- **Solution idea:** Start with a brute-force baseline, then optimize using the canonical pattern for this category.
- **Complexity target:** Improve from O(n^2) to O(n) or O(n log n) where possible.
- **Java hint:** Write a pure function, then add focused unit tests.

### Problem 304. Heap Challenge 16
- **Prompt:** Solve the problem with clear constraints and edge-case handling.
- **Solution idea:** Start with a brute-force baseline, then optimize using the canonical pattern for this category.
- **Complexity target:** Improve from O(n^2) to O(n) or O(n log n) where possible.
- **Java hint:** Write a pure function, then add focused unit tests.

### Problem 305. Heap Challenge 17
- **Prompt:** Solve the problem with clear constraints and edge-case handling.
- **Solution idea:** Start with a brute-force baseline, then optimize using the canonical pattern for this category.
- **Complexity target:** Improve from O(n^2) to O(n) or O(n log n) where possible.
- **Java hint:** Write a pure function, then add focused unit tests.

### Problem 306. Heap Challenge 18
- **Prompt:** Solve the problem with clear constraints and edge-case handling.
- **Solution idea:** Start with a brute-force baseline, then optimize using the canonical pattern for this category.
- **Complexity target:** Improve from O(n^2) to O(n) or O(n log n) where possible.
- **Java hint:** Write a pure function, then add focused unit tests.

### Problem 307. Heap Challenge 19
- **Prompt:** Solve the problem with clear constraints and edge-case handling.
- **Solution idea:** Start with a brute-force baseline, then optimize using the canonical pattern for this category.
- **Complexity target:** Improve from O(n^2) to O(n) or O(n log n) where possible.
- **Java hint:** Write a pure function, then add focused unit tests.

### Problem 308. Heap Challenge 20
- **Prompt:** Solve the problem with clear constraints and edge-case handling.
- **Solution idea:** Start with a brute-force baseline, then optimize using the canonical pattern for this category.
- **Complexity target:** Improve from O(n^2) to O(n) or O(n log n) where possible.
- **Java hint:** Write a pure function, then add focused unit tests.

### Problem 309. Heap Challenge 21
- **Prompt:** Solve the problem with clear constraints and edge-case handling.
- **Solution idea:** Start with a brute-force baseline, then optimize using the canonical pattern for this category.
- **Complexity target:** Improve from O(n^2) to O(n) or O(n log n) where possible.
- **Java hint:** Write a pure function, then add focused unit tests.

### Problem 310. Heap Challenge 22
- **Prompt:** Solve the problem with clear constraints and edge-case handling.
- **Solution idea:** Start with a brute-force baseline, then optimize using the canonical pattern for this category.
- **Complexity target:** Improve from O(n^2) to O(n) or O(n log n) where possible.
- **Java hint:** Write a pure function, then add focused unit tests.

### Problem 311. Heap Challenge 23
- **Prompt:** Solve the problem with clear constraints and edge-case handling.
- **Solution idea:** Start with a brute-force baseline, then optimize using the canonical pattern for this category.
- **Complexity target:** Improve from O(n^2) to O(n) or O(n log n) where possible.
- **Java hint:** Write a pure function, then add focused unit tests.

### Problem 312. Heap Challenge 24
- **Prompt:** Solve the problem with clear constraints and edge-case handling.
- **Solution idea:** Start with a brute-force baseline, then optimize using the canonical pattern for this category.
- **Complexity target:** Improve from O(n^2) to O(n) or O(n log n) where possible.
- **Java hint:** Write a pure function, then add focused unit tests.

### Problem 313. Heap Challenge 25
- **Prompt:** Solve the problem with clear constraints and edge-case handling.
- **Solution idea:** Start with a brute-force baseline, then optimize using the canonical pattern for this category.
- **Complexity target:** Improve from O(n^2) to O(n) or O(n log n) where possible.
- **Java hint:** Write a pure function, then add focused unit tests.

### Problem 314. Heap Challenge 26
- **Prompt:** Solve the problem with clear constraints and edge-case handling.
- **Solution idea:** Start with a brute-force baseline, then optimize using the canonical pattern for this category.
- **Complexity target:** Improve from O(n^2) to O(n) or O(n log n) where possible.
- **Java hint:** Write a pure function, then add focused unit tests.

### Problem 315. Heap Challenge 27
- **Prompt:** Solve the problem with clear constraints and edge-case handling.
- **Solution idea:** Start with a brute-force baseline, then optimize using the canonical pattern for this category.
- **Complexity target:** Improve from O(n^2) to O(n) or O(n log n) where possible.
- **Java hint:** Write a pure function, then add focused unit tests.

### Problem 316. Heap Challenge 28
- **Prompt:** Solve the problem with clear constraints and edge-case handling.
- **Solution idea:** Start with a brute-force baseline, then optimize using the canonical pattern for this category.
- **Complexity target:** Improve from O(n^2) to O(n) or O(n log n) where possible.
- **Java hint:** Write a pure function, then add focused unit tests.

### Problem 317. Heap Challenge 29
- **Prompt:** Solve the problem with clear constraints and edge-case handling.
- **Solution idea:** Start with a brute-force baseline, then optimize using the canonical pattern for this category.
- **Complexity target:** Improve from O(n^2) to O(n) or O(n log n) where possible.
- **Java hint:** Write a pure function, then add focused unit tests.

### Problem 318. Heap Challenge 30
- **Prompt:** Solve the problem with clear constraints and edge-case handling.
- **Solution idea:** Start with a brute-force baseline, then optimize using the canonical pattern for this category.
- **Complexity target:** Improve from O(n^2) to O(n) or O(n log n) where possible.
- **Java hint:** Write a pure function, then add focused unit tests.

### Problem 319. Heap Challenge 31
- **Prompt:** Solve the problem with clear constraints and edge-case handling.
- **Solution idea:** Start with a brute-force baseline, then optimize using the canonical pattern for this category.
- **Complexity target:** Improve from O(n^2) to O(n) or O(n log n) where possible.
- **Java hint:** Write a pure function, then add focused unit tests.

### Problem 320. Heap Challenge 32
- **Prompt:** Solve the problem with clear constraints and edge-case handling.
- **Solution idea:** Start with a brute-force baseline, then optimize using the canonical pattern for this category.
- **Complexity target:** Improve from O(n^2) to O(n) or O(n log n) where possible.
- **Java hint:** Write a pure function, then add focused unit tests.

## Dynamic Programming

### Problem 321. Dynamic Programming Challenge 1
- **Prompt:** Solve the problem with clear constraints and edge-case handling.
- **Solution idea:** Start with a brute-force baseline, then optimize using the canonical pattern for this category.
- **Complexity target:** Improve from O(n^2) to O(n) or O(n log n) where possible.
- **Java hint:** Write a pure function, then add focused unit tests.

### Problem 322. Dynamic Programming Challenge 2
- **Prompt:** Solve the problem with clear constraints and edge-case handling.
- **Solution idea:** Start with a brute-force baseline, then optimize using the canonical pattern for this category.
- **Complexity target:** Improve from O(n^2) to O(n) or O(n log n) where possible.
- **Java hint:** Write a pure function, then add focused unit tests.

### Problem 323. Dynamic Programming Challenge 3
- **Prompt:** Solve the problem with clear constraints and edge-case handling.
- **Solution idea:** Start with a brute-force baseline, then optimize using the canonical pattern for this category.
- **Complexity target:** Improve from O(n^2) to O(n) or O(n log n) where possible.
- **Java hint:** Write a pure function, then add focused unit tests.

### Problem 324. Dynamic Programming Challenge 4
- **Prompt:** Solve the problem with clear constraints and edge-case handling.
- **Solution idea:** Start with a brute-force baseline, then optimize using the canonical pattern for this category.
- **Complexity target:** Improve from O(n^2) to O(n) or O(n log n) where possible.
- **Java hint:** Write a pure function, then add focused unit tests.

### Problem 325. Dynamic Programming Challenge 5
- **Prompt:** Solve the problem with clear constraints and edge-case handling.
- **Solution idea:** Start with a brute-force baseline, then optimize using the canonical pattern for this category.
- **Complexity target:** Improve from O(n^2) to O(n) or O(n log n) where possible.
- **Java hint:** Write a pure function, then add focused unit tests.

### Problem 326. Dynamic Programming Challenge 6
- **Prompt:** Solve the problem with clear constraints and edge-case handling.
- **Solution idea:** Start with a brute-force baseline, then optimize using the canonical pattern for this category.
- **Complexity target:** Improve from O(n^2) to O(n) or O(n log n) where possible.
- **Java hint:** Write a pure function, then add focused unit tests.

### Problem 327. Dynamic Programming Challenge 7
- **Prompt:** Solve the problem with clear constraints and edge-case handling.
- **Solution idea:** Start with a brute-force baseline, then optimize using the canonical pattern for this category.
- **Complexity target:** Improve from O(n^2) to O(n) or O(n log n) where possible.
- **Java hint:** Write a pure function, then add focused unit tests.

### Problem 328. Dynamic Programming Challenge 8
- **Prompt:** Solve the problem with clear constraints and edge-case handling.
- **Solution idea:** Start with a brute-force baseline, then optimize using the canonical pattern for this category.
- **Complexity target:** Improve from O(n^2) to O(n) or O(n log n) where possible.
- **Java hint:** Write a pure function, then add focused unit tests.

### Problem 329. Dynamic Programming Challenge 9
- **Prompt:** Solve the problem with clear constraints and edge-case handling.
- **Solution idea:** Start with a brute-force baseline, then optimize using the canonical pattern for this category.
- **Complexity target:** Improve from O(n^2) to O(n) or O(n log n) where possible.
- **Java hint:** Write a pure function, then add focused unit tests.

### Problem 330. Dynamic Programming Challenge 10
- **Prompt:** Solve the problem with clear constraints and edge-case handling.
- **Solution idea:** Start with a brute-force baseline, then optimize using the canonical pattern for this category.
- **Complexity target:** Improve from O(n^2) to O(n) or O(n log n) where possible.
- **Java hint:** Write a pure function, then add focused unit tests.

### Problem 331. Dynamic Programming Challenge 11
- **Prompt:** Solve the problem with clear constraints and edge-case handling.
- **Solution idea:** Start with a brute-force baseline, then optimize using the canonical pattern for this category.
- **Complexity target:** Improve from O(n^2) to O(n) or O(n log n) where possible.
- **Java hint:** Write a pure function, then add focused unit tests.

### Problem 332. Dynamic Programming Challenge 12
- **Prompt:** Solve the problem with clear constraints and edge-case handling.
- **Solution idea:** Start with a brute-force baseline, then optimize using the canonical pattern for this category.
- **Complexity target:** Improve from O(n^2) to O(n) or O(n log n) where possible.
- **Java hint:** Write a pure function, then add focused unit tests.

### Problem 333. Dynamic Programming Challenge 13
- **Prompt:** Solve the problem with clear constraints and edge-case handling.
- **Solution idea:** Start with a brute-force baseline, then optimize using the canonical pattern for this category.
- **Complexity target:** Improve from O(n^2) to O(n) or O(n log n) where possible.
- **Java hint:** Write a pure function, then add focused unit tests.

### Problem 334. Dynamic Programming Challenge 14
- **Prompt:** Solve the problem with clear constraints and edge-case handling.
- **Solution idea:** Start with a brute-force baseline, then optimize using the canonical pattern for this category.
- **Complexity target:** Improve from O(n^2) to O(n) or O(n log n) where possible.
- **Java hint:** Write a pure function, then add focused unit tests.

### Problem 335. Dynamic Programming Challenge 15
- **Prompt:** Solve the problem with clear constraints and edge-case handling.
- **Solution idea:** Start with a brute-force baseline, then optimize using the canonical pattern for this category.
- **Complexity target:** Improve from O(n^2) to O(n) or O(n log n) where possible.
- **Java hint:** Write a pure function, then add focused unit tests.

### Problem 336. Dynamic Programming Challenge 16
- **Prompt:** Solve the problem with clear constraints and edge-case handling.
- **Solution idea:** Start with a brute-force baseline, then optimize using the canonical pattern for this category.
- **Complexity target:** Improve from O(n^2) to O(n) or O(n log n) where possible.
- **Java hint:** Write a pure function, then add focused unit tests.

### Problem 337. Dynamic Programming Challenge 17
- **Prompt:** Solve the problem with clear constraints and edge-case handling.
- **Solution idea:** Start with a brute-force baseline, then optimize using the canonical pattern for this category.
- **Complexity target:** Improve from O(n^2) to O(n) or O(n log n) where possible.
- **Java hint:** Write a pure function, then add focused unit tests.

### Problem 338. Dynamic Programming Challenge 18
- **Prompt:** Solve the problem with clear constraints and edge-case handling.
- **Solution idea:** Start with a brute-force baseline, then optimize using the canonical pattern for this category.
- **Complexity target:** Improve from O(n^2) to O(n) or O(n log n) where possible.
- **Java hint:** Write a pure function, then add focused unit tests.

### Problem 339. Dynamic Programming Challenge 19
- **Prompt:** Solve the problem with clear constraints and edge-case handling.
- **Solution idea:** Start with a brute-force baseline, then optimize using the canonical pattern for this category.
- **Complexity target:** Improve from O(n^2) to O(n) or O(n log n) where possible.
- **Java hint:** Write a pure function, then add focused unit tests.

### Problem 340. Dynamic Programming Challenge 20
- **Prompt:** Solve the problem with clear constraints and edge-case handling.
- **Solution idea:** Start with a brute-force baseline, then optimize using the canonical pattern for this category.
- **Complexity target:** Improve from O(n^2) to O(n) or O(n log n) where possible.
- **Java hint:** Write a pure function, then add focused unit tests.

### Problem 341. Dynamic Programming Challenge 21
- **Prompt:** Solve the problem with clear constraints and edge-case handling.
- **Solution idea:** Start with a brute-force baseline, then optimize using the canonical pattern for this category.
- **Complexity target:** Improve from O(n^2) to O(n) or O(n log n) where possible.
- **Java hint:** Write a pure function, then add focused unit tests.

### Problem 342. Dynamic Programming Challenge 22
- **Prompt:** Solve the problem with clear constraints and edge-case handling.
- **Solution idea:** Start with a brute-force baseline, then optimize using the canonical pattern for this category.
- **Complexity target:** Improve from O(n^2) to O(n) or O(n log n) where possible.
- **Java hint:** Write a pure function, then add focused unit tests.

### Problem 343. Dynamic Programming Challenge 23
- **Prompt:** Solve the problem with clear constraints and edge-case handling.
- **Solution idea:** Start with a brute-force baseline, then optimize using the canonical pattern for this category.
- **Complexity target:** Improve from O(n^2) to O(n) or O(n log n) where possible.
- **Java hint:** Write a pure function, then add focused unit tests.

### Problem 344. Dynamic Programming Challenge 24
- **Prompt:** Solve the problem with clear constraints and edge-case handling.
- **Solution idea:** Start with a brute-force baseline, then optimize using the canonical pattern for this category.
- **Complexity target:** Improve from O(n^2) to O(n) or O(n log n) where possible.
- **Java hint:** Write a pure function, then add focused unit tests.

### Problem 345. Dynamic Programming Challenge 25
- **Prompt:** Solve the problem with clear constraints and edge-case handling.
- **Solution idea:** Start with a brute-force baseline, then optimize using the canonical pattern for this category.
- **Complexity target:** Improve from O(n^2) to O(n) or O(n log n) where possible.
- **Java hint:** Write a pure function, then add focused unit tests.

### Problem 346. Dynamic Programming Challenge 26
- **Prompt:** Solve the problem with clear constraints and edge-case handling.
- **Solution idea:** Start with a brute-force baseline, then optimize using the canonical pattern for this category.
- **Complexity target:** Improve from O(n^2) to O(n) or O(n log n) where possible.
- **Java hint:** Write a pure function, then add focused unit tests.

### Problem 347. Dynamic Programming Challenge 27
- **Prompt:** Solve the problem with clear constraints and edge-case handling.
- **Solution idea:** Start with a brute-force baseline, then optimize using the canonical pattern for this category.
- **Complexity target:** Improve from O(n^2) to O(n) or O(n log n) where possible.
- **Java hint:** Write a pure function, then add focused unit tests.

### Problem 348. Dynamic Programming Challenge 28
- **Prompt:** Solve the problem with clear constraints and edge-case handling.
- **Solution idea:** Start with a brute-force baseline, then optimize using the canonical pattern for this category.
- **Complexity target:** Improve from O(n^2) to O(n) or O(n log n) where possible.
- **Java hint:** Write a pure function, then add focused unit tests.

### Problem 349. Dynamic Programming Challenge 29
- **Prompt:** Solve the problem with clear constraints and edge-case handling.
- **Solution idea:** Start with a brute-force baseline, then optimize using the canonical pattern for this category.
- **Complexity target:** Improve from O(n^2) to O(n) or O(n log n) where possible.
- **Java hint:** Write a pure function, then add focused unit tests.

### Problem 350. Dynamic Programming Challenge 30
- **Prompt:** Solve the problem with clear constraints and edge-case handling.
- **Solution idea:** Start with a brute-force baseline, then optimize using the canonical pattern for this category.
- **Complexity target:** Improve from O(n^2) to O(n) or O(n log n) where possible.
- **Java hint:** Write a pure function, then add focused unit tests.

### Problem 351. Dynamic Programming Challenge 31
- **Prompt:** Solve the problem with clear constraints and edge-case handling.
- **Solution idea:** Start with a brute-force baseline, then optimize using the canonical pattern for this category.
- **Complexity target:** Improve from O(n^2) to O(n) or O(n log n) where possible.
- **Java hint:** Write a pure function, then add focused unit tests.

### Problem 352. Dynamic Programming Challenge 32
- **Prompt:** Solve the problem with clear constraints and edge-case handling.
- **Solution idea:** Start with a brute-force baseline, then optimize using the canonical pattern for this category.
- **Complexity target:** Improve from O(n^2) to O(n) or O(n log n) where possible.
- **Java hint:** Write a pure function, then add focused unit tests.

## Sliding Window

### Problem 353. Sliding Window Challenge 1
- **Prompt:** Solve the problem with clear constraints and edge-case handling.
- **Solution idea:** Start with a brute-force baseline, then optimize using the canonical pattern for this category.
- **Complexity target:** Improve from O(n^2) to O(n) or O(n log n) where possible.
- **Java hint:** Write a pure function, then add focused unit tests.

### Problem 354. Sliding Window Challenge 2
- **Prompt:** Solve the problem with clear constraints and edge-case handling.
- **Solution idea:** Start with a brute-force baseline, then optimize using the canonical pattern for this category.
- **Complexity target:** Improve from O(n^2) to O(n) or O(n log n) where possible.
- **Java hint:** Write a pure function, then add focused unit tests.

### Problem 355. Sliding Window Challenge 3
- **Prompt:** Solve the problem with clear constraints and edge-case handling.
- **Solution idea:** Start with a brute-force baseline, then optimize using the canonical pattern for this category.
- **Complexity target:** Improve from O(n^2) to O(n) or O(n log n) where possible.
- **Java hint:** Write a pure function, then add focused unit tests.

### Problem 356. Sliding Window Challenge 4
- **Prompt:** Solve the problem with clear constraints and edge-case handling.
- **Solution idea:** Start with a brute-force baseline, then optimize using the canonical pattern for this category.
- **Complexity target:** Improve from O(n^2) to O(n) or O(n log n) where possible.
- **Java hint:** Write a pure function, then add focused unit tests.

### Problem 357. Sliding Window Challenge 5
- **Prompt:** Solve the problem with clear constraints and edge-case handling.
- **Solution idea:** Start with a brute-force baseline, then optimize using the canonical pattern for this category.
- **Complexity target:** Improve from O(n^2) to O(n) or O(n log n) where possible.
- **Java hint:** Write a pure function, then add focused unit tests.

### Problem 358. Sliding Window Challenge 6
- **Prompt:** Solve the problem with clear constraints and edge-case handling.
- **Solution idea:** Start with a brute-force baseline, then optimize using the canonical pattern for this category.
- **Complexity target:** Improve from O(n^2) to O(n) or O(n log n) where possible.
- **Java hint:** Write a pure function, then add focused unit tests.

### Problem 359. Sliding Window Challenge 7
- **Prompt:** Solve the problem with clear constraints and edge-case handling.
- **Solution idea:** Start with a brute-force baseline, then optimize using the canonical pattern for this category.
- **Complexity target:** Improve from O(n^2) to O(n) or O(n log n) where possible.
- **Java hint:** Write a pure function, then add focused unit tests.

### Problem 360. Sliding Window Challenge 8
- **Prompt:** Solve the problem with clear constraints and edge-case handling.
- **Solution idea:** Start with a brute-force baseline, then optimize using the canonical pattern for this category.
- **Complexity target:** Improve from O(n^2) to O(n) or O(n log n) where possible.
- **Java hint:** Write a pure function, then add focused unit tests.

### Problem 361. Sliding Window Challenge 9
- **Prompt:** Solve the problem with clear constraints and edge-case handling.
- **Solution idea:** Start with a brute-force baseline, then optimize using the canonical pattern for this category.
- **Complexity target:** Improve from O(n^2) to O(n) or O(n log n) where possible.
- **Java hint:** Write a pure function, then add focused unit tests.

### Problem 362. Sliding Window Challenge 10
- **Prompt:** Solve the problem with clear constraints and edge-case handling.
- **Solution idea:** Start with a brute-force baseline, then optimize using the canonical pattern for this category.
- **Complexity target:** Improve from O(n^2) to O(n) or O(n log n) where possible.
- **Java hint:** Write a pure function, then add focused unit tests.

### Problem 363. Sliding Window Challenge 11
- **Prompt:** Solve the problem with clear constraints and edge-case handling.
- **Solution idea:** Start with a brute-force baseline, then optimize using the canonical pattern for this category.
- **Complexity target:** Improve from O(n^2) to O(n) or O(n log n) where possible.
- **Java hint:** Write a pure function, then add focused unit tests.

### Problem 364. Sliding Window Challenge 12
- **Prompt:** Solve the problem with clear constraints and edge-case handling.
- **Solution idea:** Start with a brute-force baseline, then optimize using the canonical pattern for this category.
- **Complexity target:** Improve from O(n^2) to O(n) or O(n log n) where possible.
- **Java hint:** Write a pure function, then add focused unit tests.

### Problem 365. Sliding Window Challenge 13
- **Prompt:** Solve the problem with clear constraints and edge-case handling.
- **Solution idea:** Start with a brute-force baseline, then optimize using the canonical pattern for this category.
- **Complexity target:** Improve from O(n^2) to O(n) or O(n log n) where possible.
- **Java hint:** Write a pure function, then add focused unit tests.

### Problem 366. Sliding Window Challenge 14
- **Prompt:** Solve the problem with clear constraints and edge-case handling.
- **Solution idea:** Start with a brute-force baseline, then optimize using the canonical pattern for this category.
- **Complexity target:** Improve from O(n^2) to O(n) or O(n log n) where possible.
- **Java hint:** Write a pure function, then add focused unit tests.

### Problem 367. Sliding Window Challenge 15
- **Prompt:** Solve the problem with clear constraints and edge-case handling.
- **Solution idea:** Start with a brute-force baseline, then optimize using the canonical pattern for this category.
- **Complexity target:** Improve from O(n^2) to O(n) or O(n log n) where possible.
- **Java hint:** Write a pure function, then add focused unit tests.

### Problem 368. Sliding Window Challenge 16
- **Prompt:** Solve the problem with clear constraints and edge-case handling.
- **Solution idea:** Start with a brute-force baseline, then optimize using the canonical pattern for this category.
- **Complexity target:** Improve from O(n^2) to O(n) or O(n log n) where possible.
- **Java hint:** Write a pure function, then add focused unit tests.

### Problem 369. Sliding Window Challenge 17
- **Prompt:** Solve the problem with clear constraints and edge-case handling.
- **Solution idea:** Start with a brute-force baseline, then optimize using the canonical pattern for this category.
- **Complexity target:** Improve from O(n^2) to O(n) or O(n log n) where possible.
- **Java hint:** Write a pure function, then add focused unit tests.

### Problem 370. Sliding Window Challenge 18
- **Prompt:** Solve the problem with clear constraints and edge-case handling.
- **Solution idea:** Start with a brute-force baseline, then optimize using the canonical pattern for this category.
- **Complexity target:** Improve from O(n^2) to O(n) or O(n log n) where possible.
- **Java hint:** Write a pure function, then add focused unit tests.

### Problem 371. Sliding Window Challenge 19
- **Prompt:** Solve the problem with clear constraints and edge-case handling.
- **Solution idea:** Start with a brute-force baseline, then optimize using the canonical pattern for this category.
- **Complexity target:** Improve from O(n^2) to O(n) or O(n log n) where possible.
- **Java hint:** Write a pure function, then add focused unit tests.

### Problem 372. Sliding Window Challenge 20
- **Prompt:** Solve the problem with clear constraints and edge-case handling.
- **Solution idea:** Start with a brute-force baseline, then optimize using the canonical pattern for this category.
- **Complexity target:** Improve from O(n^2) to O(n) or O(n log n) where possible.
- **Java hint:** Write a pure function, then add focused unit tests.

### Problem 373. Sliding Window Challenge 21
- **Prompt:** Solve the problem with clear constraints and edge-case handling.
- **Solution idea:** Start with a brute-force baseline, then optimize using the canonical pattern for this category.
- **Complexity target:** Improve from O(n^2) to O(n) or O(n log n) where possible.
- **Java hint:** Write a pure function, then add focused unit tests.

### Problem 374. Sliding Window Challenge 22
- **Prompt:** Solve the problem with clear constraints and edge-case handling.
- **Solution idea:** Start with a brute-force baseline, then optimize using the canonical pattern for this category.
- **Complexity target:** Improve from O(n^2) to O(n) or O(n log n) where possible.
- **Java hint:** Write a pure function, then add focused unit tests.

### Problem 375. Sliding Window Challenge 23
- **Prompt:** Solve the problem with clear constraints and edge-case handling.
- **Solution idea:** Start with a brute-force baseline, then optimize using the canonical pattern for this category.
- **Complexity target:** Improve from O(n^2) to O(n) or O(n log n) where possible.
- **Java hint:** Write a pure function, then add focused unit tests.

### Problem 376. Sliding Window Challenge 24
- **Prompt:** Solve the problem with clear constraints and edge-case handling.
- **Solution idea:** Start with a brute-force baseline, then optimize using the canonical pattern for this category.
- **Complexity target:** Improve from O(n^2) to O(n) or O(n log n) where possible.
- **Java hint:** Write a pure function, then add focused unit tests.

### Problem 377. Sliding Window Challenge 25
- **Prompt:** Solve the problem with clear constraints and edge-case handling.
- **Solution idea:** Start with a brute-force baseline, then optimize using the canonical pattern for this category.
- **Complexity target:** Improve from O(n^2) to O(n) or O(n log n) where possible.
- **Java hint:** Write a pure function, then add focused unit tests.

### Problem 378. Sliding Window Challenge 26
- **Prompt:** Solve the problem with clear constraints and edge-case handling.
- **Solution idea:** Start with a brute-force baseline, then optimize using the canonical pattern for this category.
- **Complexity target:** Improve from O(n^2) to O(n) or O(n log n) where possible.
- **Java hint:** Write a pure function, then add focused unit tests.

### Problem 379. Sliding Window Challenge 27
- **Prompt:** Solve the problem with clear constraints and edge-case handling.
- **Solution idea:** Start with a brute-force baseline, then optimize using the canonical pattern for this category.
- **Complexity target:** Improve from O(n^2) to O(n) or O(n log n) where possible.
- **Java hint:** Write a pure function, then add focused unit tests.

### Problem 380. Sliding Window Challenge 28
- **Prompt:** Solve the problem with clear constraints and edge-case handling.
- **Solution idea:** Start with a brute-force baseline, then optimize using the canonical pattern for this category.
- **Complexity target:** Improve from O(n^2) to O(n) or O(n log n) where possible.
- **Java hint:** Write a pure function, then add focused unit tests.

### Problem 381. Sliding Window Challenge 29
- **Prompt:** Solve the problem with clear constraints and edge-case handling.
- **Solution idea:** Start with a brute-force baseline, then optimize using the canonical pattern for this category.
- **Complexity target:** Improve from O(n^2) to O(n) or O(n log n) where possible.
- **Java hint:** Write a pure function, then add focused unit tests.

### Problem 382. Sliding Window Challenge 30
- **Prompt:** Solve the problem with clear constraints and edge-case handling.
- **Solution idea:** Start with a brute-force baseline, then optimize using the canonical pattern for this category.
- **Complexity target:** Improve from O(n^2) to O(n) or O(n log n) where possible.
- **Java hint:** Write a pure function, then add focused unit tests.

### Problem 383. Sliding Window Challenge 31
- **Prompt:** Solve the problem with clear constraints and edge-case handling.
- **Solution idea:** Start with a brute-force baseline, then optimize using the canonical pattern for this category.
- **Complexity target:** Improve from O(n^2) to O(n) or O(n log n) where possible.
- **Java hint:** Write a pure function, then add focused unit tests.

### Problem 384. Sliding Window Challenge 32
- **Prompt:** Solve the problem with clear constraints and edge-case handling.
- **Solution idea:** Start with a brute-force baseline, then optimize using the canonical pattern for this category.
- **Complexity target:** Improve from O(n^2) to O(n) or O(n log n) where possible.
- **Java hint:** Write a pure function, then add focused unit tests.

## Two Pointer

### Problem 385. Two Pointer Challenge 1
- **Prompt:** Solve the problem with clear constraints and edge-case handling.
- **Solution idea:** Start with a brute-force baseline, then optimize using the canonical pattern for this category.
- **Complexity target:** Improve from O(n^2) to O(n) or O(n log n) where possible.
- **Java hint:** Write a pure function, then add focused unit tests.

### Problem 386. Two Pointer Challenge 2
- **Prompt:** Solve the problem with clear constraints and edge-case handling.
- **Solution idea:** Start with a brute-force baseline, then optimize using the canonical pattern for this category.
- **Complexity target:** Improve from O(n^2) to O(n) or O(n log n) where possible.
- **Java hint:** Write a pure function, then add focused unit tests.

### Problem 387. Two Pointer Challenge 3
- **Prompt:** Solve the problem with clear constraints and edge-case handling.
- **Solution idea:** Start with a brute-force baseline, then optimize using the canonical pattern for this category.
- **Complexity target:** Improve from O(n^2) to O(n) or O(n log n) where possible.
- **Java hint:** Write a pure function, then add focused unit tests.

### Problem 388. Two Pointer Challenge 4
- **Prompt:** Solve the problem with clear constraints and edge-case handling.
- **Solution idea:** Start with a brute-force baseline, then optimize using the canonical pattern for this category.
- **Complexity target:** Improve from O(n^2) to O(n) or O(n log n) where possible.
- **Java hint:** Write a pure function, then add focused unit tests.

### Problem 389. Two Pointer Challenge 5
- **Prompt:** Solve the problem with clear constraints and edge-case handling.
- **Solution idea:** Start with a brute-force baseline, then optimize using the canonical pattern for this category.
- **Complexity target:** Improve from O(n^2) to O(n) or O(n log n) where possible.
- **Java hint:** Write a pure function, then add focused unit tests.

### Problem 390. Two Pointer Challenge 6
- **Prompt:** Solve the problem with clear constraints and edge-case handling.
- **Solution idea:** Start with a brute-force baseline, then optimize using the canonical pattern for this category.
- **Complexity target:** Improve from O(n^2) to O(n) or O(n log n) where possible.
- **Java hint:** Write a pure function, then add focused unit tests.

### Problem 391. Two Pointer Challenge 7
- **Prompt:** Solve the problem with clear constraints and edge-case handling.
- **Solution idea:** Start with a brute-force baseline, then optimize using the canonical pattern for this category.
- **Complexity target:** Improve from O(n^2) to O(n) or O(n log n) where possible.
- **Java hint:** Write a pure function, then add focused unit tests.

### Problem 392. Two Pointer Challenge 8
- **Prompt:** Solve the problem with clear constraints and edge-case handling.
- **Solution idea:** Start with a brute-force baseline, then optimize using the canonical pattern for this category.
- **Complexity target:** Improve from O(n^2) to O(n) or O(n log n) where possible.
- **Java hint:** Write a pure function, then add focused unit tests.

### Problem 393. Two Pointer Challenge 9
- **Prompt:** Solve the problem with clear constraints and edge-case handling.
- **Solution idea:** Start with a brute-force baseline, then optimize using the canonical pattern for this category.
- **Complexity target:** Improve from O(n^2) to O(n) or O(n log n) where possible.
- **Java hint:** Write a pure function, then add focused unit tests.

### Problem 394. Two Pointer Challenge 10
- **Prompt:** Solve the problem with clear constraints and edge-case handling.
- **Solution idea:** Start with a brute-force baseline, then optimize using the canonical pattern for this category.
- **Complexity target:** Improve from O(n^2) to O(n) or O(n log n) where possible.
- **Java hint:** Write a pure function, then add focused unit tests.

### Problem 395. Two Pointer Challenge 11
- **Prompt:** Solve the problem with clear constraints and edge-case handling.
- **Solution idea:** Start with a brute-force baseline, then optimize using the canonical pattern for this category.
- **Complexity target:** Improve from O(n^2) to O(n) or O(n log n) where possible.
- **Java hint:** Write a pure function, then add focused unit tests.

### Problem 396. Two Pointer Challenge 12
- **Prompt:** Solve the problem with clear constraints and edge-case handling.
- **Solution idea:** Start with a brute-force baseline, then optimize using the canonical pattern for this category.
- **Complexity target:** Improve from O(n^2) to O(n) or O(n log n) where possible.
- **Java hint:** Write a pure function, then add focused unit tests.

### Problem 397. Two Pointer Challenge 13
- **Prompt:** Solve the problem with clear constraints and edge-case handling.
- **Solution idea:** Start with a brute-force baseline, then optimize using the canonical pattern for this category.
- **Complexity target:** Improve from O(n^2) to O(n) or O(n log n) where possible.
- **Java hint:** Write a pure function, then add focused unit tests.

### Problem 398. Two Pointer Challenge 14
- **Prompt:** Solve the problem with clear constraints and edge-case handling.
- **Solution idea:** Start with a brute-force baseline, then optimize using the canonical pattern for this category.
- **Complexity target:** Improve from O(n^2) to O(n) or O(n log n) where possible.
- **Java hint:** Write a pure function, then add focused unit tests.

### Problem 399. Two Pointer Challenge 15
- **Prompt:** Solve the problem with clear constraints and edge-case handling.
- **Solution idea:** Start with a brute-force baseline, then optimize using the canonical pattern for this category.
- **Complexity target:** Improve from O(n^2) to O(n) or O(n log n) where possible.
- **Java hint:** Write a pure function, then add focused unit tests.

### Problem 400. Two Pointer Challenge 16
- **Prompt:** Solve the problem with clear constraints and edge-case handling.
- **Solution idea:** Start with a brute-force baseline, then optimize using the canonical pattern for this category.
- **Complexity target:** Improve from O(n^2) to O(n) or O(n log n) where possible.
- **Java hint:** Write a pure function, then add focused unit tests.

### Problem 401. Two Pointer Challenge 17
- **Prompt:** Solve the problem with clear constraints and edge-case handling.
- **Solution idea:** Start with a brute-force baseline, then optimize using the canonical pattern for this category.
- **Complexity target:** Improve from O(n^2) to O(n) or O(n log n) where possible.
- **Java hint:** Write a pure function, then add focused unit tests.

### Problem 402. Two Pointer Challenge 18
- **Prompt:** Solve the problem with clear constraints and edge-case handling.
- **Solution idea:** Start with a brute-force baseline, then optimize using the canonical pattern for this category.
- **Complexity target:** Improve from O(n^2) to O(n) or O(n log n) where possible.
- **Java hint:** Write a pure function, then add focused unit tests.

### Problem 403. Two Pointer Challenge 19
- **Prompt:** Solve the problem with clear constraints and edge-case handling.
- **Solution idea:** Start with a brute-force baseline, then optimize using the canonical pattern for this category.
- **Complexity target:** Improve from O(n^2) to O(n) or O(n log n) where possible.
- **Java hint:** Write a pure function, then add focused unit tests.

### Problem 404. Two Pointer Challenge 20
- **Prompt:** Solve the problem with clear constraints and edge-case handling.
- **Solution idea:** Start with a brute-force baseline, then optimize using the canonical pattern for this category.
- **Complexity target:** Improve from O(n^2) to O(n) or O(n log n) where possible.
- **Java hint:** Write a pure function, then add focused unit tests.

### Problem 405. Two Pointer Challenge 21
- **Prompt:** Solve the problem with clear constraints and edge-case handling.
- **Solution idea:** Start with a brute-force baseline, then optimize using the canonical pattern for this category.
- **Complexity target:** Improve from O(n^2) to O(n) or O(n log n) where possible.
- **Java hint:** Write a pure function, then add focused unit tests.

### Problem 406. Two Pointer Challenge 22
- **Prompt:** Solve the problem with clear constraints and edge-case handling.
- **Solution idea:** Start with a brute-force baseline, then optimize using the canonical pattern for this category.
- **Complexity target:** Improve from O(n^2) to O(n) or O(n log n) where possible.
- **Java hint:** Write a pure function, then add focused unit tests.

### Problem 407. Two Pointer Challenge 23
- **Prompt:** Solve the problem with clear constraints and edge-case handling.
- **Solution idea:** Start with a brute-force baseline, then optimize using the canonical pattern for this category.
- **Complexity target:** Improve from O(n^2) to O(n) or O(n log n) where possible.
- **Java hint:** Write a pure function, then add focused unit tests.

### Problem 408. Two Pointer Challenge 24
- **Prompt:** Solve the problem with clear constraints and edge-case handling.
- **Solution idea:** Start with a brute-force baseline, then optimize using the canonical pattern for this category.
- **Complexity target:** Improve from O(n^2) to O(n) or O(n log n) where possible.
- **Java hint:** Write a pure function, then add focused unit tests.

### Problem 409. Two Pointer Challenge 25
- **Prompt:** Solve the problem with clear constraints and edge-case handling.
- **Solution idea:** Start with a brute-force baseline, then optimize using the canonical pattern for this category.
- **Complexity target:** Improve from O(n^2) to O(n) or O(n log n) where possible.
- **Java hint:** Write a pure function, then add focused unit tests.

### Problem 410. Two Pointer Challenge 26
- **Prompt:** Solve the problem with clear constraints and edge-case handling.
- **Solution idea:** Start with a brute-force baseline, then optimize using the canonical pattern for this category.
- **Complexity target:** Improve from O(n^2) to O(n) or O(n log n) where possible.
- **Java hint:** Write a pure function, then add focused unit tests.

### Problem 411. Two Pointer Challenge 27
- **Prompt:** Solve the problem with clear constraints and edge-case handling.
- **Solution idea:** Start with a brute-force baseline, then optimize using the canonical pattern for this category.
- **Complexity target:** Improve from O(n^2) to O(n) or O(n log n) where possible.
- **Java hint:** Write a pure function, then add focused unit tests.

### Problem 412. Two Pointer Challenge 28
- **Prompt:** Solve the problem with clear constraints and edge-case handling.
- **Solution idea:** Start with a brute-force baseline, then optimize using the canonical pattern for this category.
- **Complexity target:** Improve from O(n^2) to O(n) or O(n log n) where possible.
- **Java hint:** Write a pure function, then add focused unit tests.

### Problem 413. Two Pointer Challenge 29
- **Prompt:** Solve the problem with clear constraints and edge-case handling.
- **Solution idea:** Start with a brute-force baseline, then optimize using the canonical pattern for this category.
- **Complexity target:** Improve from O(n^2) to O(n) or O(n log n) where possible.
- **Java hint:** Write a pure function, then add focused unit tests.

### Problem 414. Two Pointer Challenge 30
- **Prompt:** Solve the problem with clear constraints and edge-case handling.
- **Solution idea:** Start with a brute-force baseline, then optimize using the canonical pattern for this category.
- **Complexity target:** Improve from O(n^2) to O(n) or O(n log n) where possible.
- **Java hint:** Write a pure function, then add focused unit tests.

### Problem 415. Two Pointer Challenge 31
- **Prompt:** Solve the problem with clear constraints and edge-case handling.
- **Solution idea:** Start with a brute-force baseline, then optimize using the canonical pattern for this category.
- **Complexity target:** Improve from O(n^2) to O(n) or O(n log n) where possible.
- **Java hint:** Write a pure function, then add focused unit tests.

### Problem 416. Two Pointer Challenge 32
- **Prompt:** Solve the problem with clear constraints and edge-case handling.
- **Solution idea:** Start with a brute-force baseline, then optimize using the canonical pattern for this category.
- **Complexity target:** Improve from O(n^2) to O(n) or O(n log n) where possible.
- **Java hint:** Write a pure function, then add focused unit tests.

## Backtracking

### Problem 417. Backtracking Challenge 1
- **Prompt:** Solve the problem with clear constraints and edge-case handling.
- **Solution idea:** Start with a brute-force baseline, then optimize using the canonical pattern for this category.
- **Complexity target:** Improve from O(n^2) to O(n) or O(n log n) where possible.
- **Java hint:** Write a pure function, then add focused unit tests.

### Problem 418. Backtracking Challenge 2
- **Prompt:** Solve the problem with clear constraints and edge-case handling.
- **Solution idea:** Start with a brute-force baseline, then optimize using the canonical pattern for this category.
- **Complexity target:** Improve from O(n^2) to O(n) or O(n log n) where possible.
- **Java hint:** Write a pure function, then add focused unit tests.

### Problem 419. Backtracking Challenge 3
- **Prompt:** Solve the problem with clear constraints and edge-case handling.
- **Solution idea:** Start with a brute-force baseline, then optimize using the canonical pattern for this category.
- **Complexity target:** Improve from O(n^2) to O(n) or O(n log n) where possible.
- **Java hint:** Write a pure function, then add focused unit tests.

### Problem 420. Backtracking Challenge 4
- **Prompt:** Solve the problem with clear constraints and edge-case handling.
- **Solution idea:** Start with a brute-force baseline, then optimize using the canonical pattern for this category.
- **Complexity target:** Improve from O(n^2) to O(n) or O(n log n) where possible.
- **Java hint:** Write a pure function, then add focused unit tests.

### Problem 421. Backtracking Challenge 5
- **Prompt:** Solve the problem with clear constraints and edge-case handling.
- **Solution idea:** Start with a brute-force baseline, then optimize using the canonical pattern for this category.
- **Complexity target:** Improve from O(n^2) to O(n) or O(n log n) where possible.
- **Java hint:** Write a pure function, then add focused unit tests.

### Problem 422. Backtracking Challenge 6
- **Prompt:** Solve the problem with clear constraints and edge-case handling.
- **Solution idea:** Start with a brute-force baseline, then optimize using the canonical pattern for this category.
- **Complexity target:** Improve from O(n^2) to O(n) or O(n log n) where possible.
- **Java hint:** Write a pure function, then add focused unit tests.

### Problem 423. Backtracking Challenge 7
- **Prompt:** Solve the problem with clear constraints and edge-case handling.
- **Solution idea:** Start with a brute-force baseline, then optimize using the canonical pattern for this category.
- **Complexity target:** Improve from O(n^2) to O(n) or O(n log n) where possible.
- **Java hint:** Write a pure function, then add focused unit tests.

### Problem 424. Backtracking Challenge 8
- **Prompt:** Solve the problem with clear constraints and edge-case handling.
- **Solution idea:** Start with a brute-force baseline, then optimize using the canonical pattern for this category.
- **Complexity target:** Improve from O(n^2) to O(n) or O(n log n) where possible.
- **Java hint:** Write a pure function, then add focused unit tests.

### Problem 425. Backtracking Challenge 9
- **Prompt:** Solve the problem with clear constraints and edge-case handling.
- **Solution idea:** Start with a brute-force baseline, then optimize using the canonical pattern for this category.
- **Complexity target:** Improve from O(n^2) to O(n) or O(n log n) where possible.
- **Java hint:** Write a pure function, then add focused unit tests.

### Problem 426. Backtracking Challenge 10
- **Prompt:** Solve the problem with clear constraints and edge-case handling.
- **Solution idea:** Start with a brute-force baseline, then optimize using the canonical pattern for this category.
- **Complexity target:** Improve from O(n^2) to O(n) or O(n log n) where possible.
- **Java hint:** Write a pure function, then add focused unit tests.

### Problem 427. Backtracking Challenge 11
- **Prompt:** Solve the problem with clear constraints and edge-case handling.
- **Solution idea:** Start with a brute-force baseline, then optimize using the canonical pattern for this category.
- **Complexity target:** Improve from O(n^2) to O(n) or O(n log n) where possible.
- **Java hint:** Write a pure function, then add focused unit tests.

### Problem 428. Backtracking Challenge 12
- **Prompt:** Solve the problem with clear constraints and edge-case handling.
- **Solution idea:** Start with a brute-force baseline, then optimize using the canonical pattern for this category.
- **Complexity target:** Improve from O(n^2) to O(n) or O(n log n) where possible.
- **Java hint:** Write a pure function, then add focused unit tests.

### Problem 429. Backtracking Challenge 13
- **Prompt:** Solve the problem with clear constraints and edge-case handling.
- **Solution idea:** Start with a brute-force baseline, then optimize using the canonical pattern for this category.
- **Complexity target:** Improve from O(n^2) to O(n) or O(n log n) where possible.
- **Java hint:** Write a pure function, then add focused unit tests.

### Problem 430. Backtracking Challenge 14
- **Prompt:** Solve the problem with clear constraints and edge-case handling.
- **Solution idea:** Start with a brute-force baseline, then optimize using the canonical pattern for this category.
- **Complexity target:** Improve from O(n^2) to O(n) or O(n log n) where possible.
- **Java hint:** Write a pure function, then add focused unit tests.

### Problem 431. Backtracking Challenge 15
- **Prompt:** Solve the problem with clear constraints and edge-case handling.
- **Solution idea:** Start with a brute-force baseline, then optimize using the canonical pattern for this category.
- **Complexity target:** Improve from O(n^2) to O(n) or O(n log n) where possible.
- **Java hint:** Write a pure function, then add focused unit tests.

### Problem 432. Backtracking Challenge 16
- **Prompt:** Solve the problem with clear constraints and edge-case handling.
- **Solution idea:** Start with a brute-force baseline, then optimize using the canonical pattern for this category.
- **Complexity target:** Improve from O(n^2) to O(n) or O(n log n) where possible.
- **Java hint:** Write a pure function, then add focused unit tests.

### Problem 433. Backtracking Challenge 17
- **Prompt:** Solve the problem with clear constraints and edge-case handling.
- **Solution idea:** Start with a brute-force baseline, then optimize using the canonical pattern for this category.
- **Complexity target:** Improve from O(n^2) to O(n) or O(n log n) where possible.
- **Java hint:** Write a pure function, then add focused unit tests.

### Problem 434. Backtracking Challenge 18
- **Prompt:** Solve the problem with clear constraints and edge-case handling.
- **Solution idea:** Start with a brute-force baseline, then optimize using the canonical pattern for this category.
- **Complexity target:** Improve from O(n^2) to O(n) or O(n log n) where possible.
- **Java hint:** Write a pure function, then add focused unit tests.

### Problem 435. Backtracking Challenge 19
- **Prompt:** Solve the problem with clear constraints and edge-case handling.
- **Solution idea:** Start with a brute-force baseline, then optimize using the canonical pattern for this category.
- **Complexity target:** Improve from O(n^2) to O(n) or O(n log n) where possible.
- **Java hint:** Write a pure function, then add focused unit tests.

### Problem 436. Backtracking Challenge 20
- **Prompt:** Solve the problem with clear constraints and edge-case handling.
- **Solution idea:** Start with a brute-force baseline, then optimize using the canonical pattern for this category.
- **Complexity target:** Improve from O(n^2) to O(n) or O(n log n) where possible.
- **Java hint:** Write a pure function, then add focused unit tests.

### Problem 437. Backtracking Challenge 21
- **Prompt:** Solve the problem with clear constraints and edge-case handling.
- **Solution idea:** Start with a brute-force baseline, then optimize using the canonical pattern for this category.
- **Complexity target:** Improve from O(n^2) to O(n) or O(n log n) where possible.
- **Java hint:** Write a pure function, then add focused unit tests.

### Problem 438. Backtracking Challenge 22
- **Prompt:** Solve the problem with clear constraints and edge-case handling.
- **Solution idea:** Start with a brute-force baseline, then optimize using the canonical pattern for this category.
- **Complexity target:** Improve from O(n^2) to O(n) or O(n log n) where possible.
- **Java hint:** Write a pure function, then add focused unit tests.

### Problem 439. Backtracking Challenge 23
- **Prompt:** Solve the problem with clear constraints and edge-case handling.
- **Solution idea:** Start with a brute-force baseline, then optimize using the canonical pattern for this category.
- **Complexity target:** Improve from O(n^2) to O(n) or O(n log n) where possible.
- **Java hint:** Write a pure function, then add focused unit tests.

### Problem 440. Backtracking Challenge 24
- **Prompt:** Solve the problem with clear constraints and edge-case handling.
- **Solution idea:** Start with a brute-force baseline, then optimize using the canonical pattern for this category.
- **Complexity target:** Improve from O(n^2) to O(n) or O(n log n) where possible.
- **Java hint:** Write a pure function, then add focused unit tests.

### Problem 441. Backtracking Challenge 25
- **Prompt:** Solve the problem with clear constraints and edge-case handling.
- **Solution idea:** Start with a brute-force baseline, then optimize using the canonical pattern for this category.
- **Complexity target:** Improve from O(n^2) to O(n) or O(n log n) where possible.
- **Java hint:** Write a pure function, then add focused unit tests.

### Problem 442. Backtracking Challenge 26
- **Prompt:** Solve the problem with clear constraints and edge-case handling.
- **Solution idea:** Start with a brute-force baseline, then optimize using the canonical pattern for this category.
- **Complexity target:** Improve from O(n^2) to O(n) or O(n log n) where possible.
- **Java hint:** Write a pure function, then add focused unit tests.

### Problem 443. Backtracking Challenge 27
- **Prompt:** Solve the problem with clear constraints and edge-case handling.
- **Solution idea:** Start with a brute-force baseline, then optimize using the canonical pattern for this category.
- **Complexity target:** Improve from O(n^2) to O(n) or O(n log n) where possible.
- **Java hint:** Write a pure function, then add focused unit tests.

### Problem 444. Backtracking Challenge 28
- **Prompt:** Solve the problem with clear constraints and edge-case handling.
- **Solution idea:** Start with a brute-force baseline, then optimize using the canonical pattern for this category.
- **Complexity target:** Improve from O(n^2) to O(n) or O(n log n) where possible.
- **Java hint:** Write a pure function, then add focused unit tests.

### Problem 445. Backtracking Challenge 29
- **Prompt:** Solve the problem with clear constraints and edge-case handling.
- **Solution idea:** Start with a brute-force baseline, then optimize using the canonical pattern for this category.
- **Complexity target:** Improve from O(n^2) to O(n) or O(n log n) where possible.
- **Java hint:** Write a pure function, then add focused unit tests.

### Problem 446. Backtracking Challenge 30
- **Prompt:** Solve the problem with clear constraints and edge-case handling.
- **Solution idea:** Start with a brute-force baseline, then optimize using the canonical pattern for this category.
- **Complexity target:** Improve from O(n^2) to O(n) or O(n log n) where possible.
- **Java hint:** Write a pure function, then add focused unit tests.

### Problem 447. Backtracking Challenge 31
- **Prompt:** Solve the problem with clear constraints and edge-case handling.
- **Solution idea:** Start with a brute-force baseline, then optimize using the canonical pattern for this category.
- **Complexity target:** Improve from O(n^2) to O(n) or O(n log n) where possible.
- **Java hint:** Write a pure function, then add focused unit tests.

### Problem 448. Backtracking Challenge 32
- **Prompt:** Solve the problem with clear constraints and edge-case handling.
- **Solution idea:** Start with a brute-force baseline, then optimize using the canonical pattern for this category.
- **Complexity target:** Improve from O(n^2) to O(n) or O(n log n) where possible.
- **Java hint:** Write a pure function, then add focused unit tests.

## Java Streams Problems

### Problem 449. Java Streams Problems Challenge 1
- **Prompt:** Solve the problem with clear constraints and edge-case handling.
- **Solution idea:** Start with a brute-force baseline, then optimize using the canonical pattern for this category.
- **Complexity target:** Improve from O(n^2) to O(n) or O(n log n) where possible.
- **Java hint:** Write a pure function, then add focused unit tests.

### Problem 450. Java Streams Problems Challenge 2
- **Prompt:** Solve the problem with clear constraints and edge-case handling.
- **Solution idea:** Start with a brute-force baseline, then optimize using the canonical pattern for this category.
- **Complexity target:** Improve from O(n^2) to O(n) or O(n log n) where possible.
- **Java hint:** Write a pure function, then add focused unit tests.

### Problem 451. Java Streams Problems Challenge 3
- **Prompt:** Solve the problem with clear constraints and edge-case handling.
- **Solution idea:** Start with a brute-force baseline, then optimize using the canonical pattern for this category.
- **Complexity target:** Improve from O(n^2) to O(n) or O(n log n) where possible.
- **Java hint:** Write a pure function, then add focused unit tests.

### Problem 452. Java Streams Problems Challenge 4
- **Prompt:** Solve the problem with clear constraints and edge-case handling.
- **Solution idea:** Start with a brute-force baseline, then optimize using the canonical pattern for this category.
- **Complexity target:** Improve from O(n^2) to O(n) or O(n log n) where possible.
- **Java hint:** Write a pure function, then add focused unit tests.

### Problem 453. Java Streams Problems Challenge 5
- **Prompt:** Solve the problem with clear constraints and edge-case handling.
- **Solution idea:** Start with a brute-force baseline, then optimize using the canonical pattern for this category.
- **Complexity target:** Improve from O(n^2) to O(n) or O(n log n) where possible.
- **Java hint:** Write a pure function, then add focused unit tests.

### Problem 454. Java Streams Problems Challenge 6
- **Prompt:** Solve the problem with clear constraints and edge-case handling.
- **Solution idea:** Start with a brute-force baseline, then optimize using the canonical pattern for this category.
- **Complexity target:** Improve from O(n^2) to O(n) or O(n log n) where possible.
- **Java hint:** Write a pure function, then add focused unit tests.

### Problem 455. Java Streams Problems Challenge 7
- **Prompt:** Solve the problem with clear constraints and edge-case handling.
- **Solution idea:** Start with a brute-force baseline, then optimize using the canonical pattern for this category.
- **Complexity target:** Improve from O(n^2) to O(n) or O(n log n) where possible.
- **Java hint:** Write a pure function, then add focused unit tests.

### Problem 456. Java Streams Problems Challenge 8
- **Prompt:** Solve the problem with clear constraints and edge-case handling.
- **Solution idea:** Start with a brute-force baseline, then optimize using the canonical pattern for this category.
- **Complexity target:** Improve from O(n^2) to O(n) or O(n log n) where possible.
- **Java hint:** Write a pure function, then add focused unit tests.

### Problem 457. Java Streams Problems Challenge 9
- **Prompt:** Solve the problem with clear constraints and edge-case handling.
- **Solution idea:** Start with a brute-force baseline, then optimize using the canonical pattern for this category.
- **Complexity target:** Improve from O(n^2) to O(n) or O(n log n) where possible.
- **Java hint:** Write a pure function, then add focused unit tests.

### Problem 458. Java Streams Problems Challenge 10
- **Prompt:** Solve the problem with clear constraints and edge-case handling.
- **Solution idea:** Start with a brute-force baseline, then optimize using the canonical pattern for this category.
- **Complexity target:** Improve from O(n^2) to O(n) or O(n log n) where possible.
- **Java hint:** Write a pure function, then add focused unit tests.

### Problem 459. Java Streams Problems Challenge 11
- **Prompt:** Solve the problem with clear constraints and edge-case handling.
- **Solution idea:** Start with a brute-force baseline, then optimize using the canonical pattern for this category.
- **Complexity target:** Improve from O(n^2) to O(n) or O(n log n) where possible.
- **Java hint:** Write a pure function, then add focused unit tests.

### Problem 460. Java Streams Problems Challenge 12
- **Prompt:** Solve the problem with clear constraints and edge-case handling.
- **Solution idea:** Start with a brute-force baseline, then optimize using the canonical pattern for this category.
- **Complexity target:** Improve from O(n^2) to O(n) or O(n log n) where possible.
- **Java hint:** Write a pure function, then add focused unit tests.

### Problem 461. Java Streams Problems Challenge 13
- **Prompt:** Solve the problem with clear constraints and edge-case handling.
- **Solution idea:** Start with a brute-force baseline, then optimize using the canonical pattern for this category.
- **Complexity target:** Improve from O(n^2) to O(n) or O(n log n) where possible.
- **Java hint:** Write a pure function, then add focused unit tests.

### Problem 462. Java Streams Problems Challenge 14
- **Prompt:** Solve the problem with clear constraints and edge-case handling.
- **Solution idea:** Start with a brute-force baseline, then optimize using the canonical pattern for this category.
- **Complexity target:** Improve from O(n^2) to O(n) or O(n log n) where possible.
- **Java hint:** Write a pure function, then add focused unit tests.

### Problem 463. Java Streams Problems Challenge 15
- **Prompt:** Solve the problem with clear constraints and edge-case handling.
- **Solution idea:** Start with a brute-force baseline, then optimize using the canonical pattern for this category.
- **Complexity target:** Improve from O(n^2) to O(n) or O(n log n) where possible.
- **Java hint:** Write a pure function, then add focused unit tests.

### Problem 464. Java Streams Problems Challenge 16
- **Prompt:** Solve the problem with clear constraints and edge-case handling.
- **Solution idea:** Start with a brute-force baseline, then optimize using the canonical pattern for this category.
- **Complexity target:** Improve from O(n^2) to O(n) or O(n log n) where possible.
- **Java hint:** Write a pure function, then add focused unit tests.

### Problem 465. Java Streams Problems Challenge 17
- **Prompt:** Solve the problem with clear constraints and edge-case handling.
- **Solution idea:** Start with a brute-force baseline, then optimize using the canonical pattern for this category.
- **Complexity target:** Improve from O(n^2) to O(n) or O(n log n) where possible.
- **Java hint:** Write a pure function, then add focused unit tests.

### Problem 466. Java Streams Problems Challenge 18
- **Prompt:** Solve the problem with clear constraints and edge-case handling.
- **Solution idea:** Start with a brute-force baseline, then optimize using the canonical pattern for this category.
- **Complexity target:** Improve from O(n^2) to O(n) or O(n log n) where possible.
- **Java hint:** Write a pure function, then add focused unit tests.

### Problem 467. Java Streams Problems Challenge 19
- **Prompt:** Solve the problem with clear constraints and edge-case handling.
- **Solution idea:** Start with a brute-force baseline, then optimize using the canonical pattern for this category.
- **Complexity target:** Improve from O(n^2) to O(n) or O(n log n) where possible.
- **Java hint:** Write a pure function, then add focused unit tests.

### Problem 468. Java Streams Problems Challenge 20
- **Prompt:** Solve the problem with clear constraints and edge-case handling.
- **Solution idea:** Start with a brute-force baseline, then optimize using the canonical pattern for this category.
- **Complexity target:** Improve from O(n^2) to O(n) or O(n log n) where possible.
- **Java hint:** Write a pure function, then add focused unit tests.

### Problem 469. Java Streams Problems Challenge 21
- **Prompt:** Solve the problem with clear constraints and edge-case handling.
- **Solution idea:** Start with a brute-force baseline, then optimize using the canonical pattern for this category.
- **Complexity target:** Improve from O(n^2) to O(n) or O(n log n) where possible.
- **Java hint:** Write a pure function, then add focused unit tests.

### Problem 470. Java Streams Problems Challenge 22
- **Prompt:** Solve the problem with clear constraints and edge-case handling.
- **Solution idea:** Start with a brute-force baseline, then optimize using the canonical pattern for this category.
- **Complexity target:** Improve from O(n^2) to O(n) or O(n log n) where possible.
- **Java hint:** Write a pure function, then add focused unit tests.

### Problem 471. Java Streams Problems Challenge 23
- **Prompt:** Solve the problem with clear constraints and edge-case handling.
- **Solution idea:** Start with a brute-force baseline, then optimize using the canonical pattern for this category.
- **Complexity target:** Improve from O(n^2) to O(n) or O(n log n) where possible.
- **Java hint:** Write a pure function, then add focused unit tests.

### Problem 472. Java Streams Problems Challenge 24
- **Prompt:** Solve the problem with clear constraints and edge-case handling.
- **Solution idea:** Start with a brute-force baseline, then optimize using the canonical pattern for this category.
- **Complexity target:** Improve from O(n^2) to O(n) or O(n log n) where possible.
- **Java hint:** Write a pure function, then add focused unit tests.

### Problem 473. Java Streams Problems Challenge 25
- **Prompt:** Solve the problem with clear constraints and edge-case handling.
- **Solution idea:** Start with a brute-force baseline, then optimize using the canonical pattern for this category.
- **Complexity target:** Improve from O(n^2) to O(n) or O(n log n) where possible.
- **Java hint:** Write a pure function, then add focused unit tests.

### Problem 474. Java Streams Problems Challenge 26
- **Prompt:** Solve the problem with clear constraints and edge-case handling.
- **Solution idea:** Start with a brute-force baseline, then optimize using the canonical pattern for this category.
- **Complexity target:** Improve from O(n^2) to O(n) or O(n log n) where possible.
- **Java hint:** Write a pure function, then add focused unit tests.

### Problem 475. Java Streams Problems Challenge 27
- **Prompt:** Solve the problem with clear constraints and edge-case handling.
- **Solution idea:** Start with a brute-force baseline, then optimize using the canonical pattern for this category.
- **Complexity target:** Improve from O(n^2) to O(n) or O(n log n) where possible.
- **Java hint:** Write a pure function, then add focused unit tests.

### Problem 476. Java Streams Problems Challenge 28
- **Prompt:** Solve the problem with clear constraints and edge-case handling.
- **Solution idea:** Start with a brute-force baseline, then optimize using the canonical pattern for this category.
- **Complexity target:** Improve from O(n^2) to O(n) or O(n log n) where possible.
- **Java hint:** Write a pure function, then add focused unit tests.

### Problem 477. Java Streams Problems Challenge 29
- **Prompt:** Solve the problem with clear constraints and edge-case handling.
- **Solution idea:** Start with a brute-force baseline, then optimize using the canonical pattern for this category.
- **Complexity target:** Improve from O(n^2) to O(n) or O(n log n) where possible.
- **Java hint:** Write a pure function, then add focused unit tests.

### Problem 478. Java Streams Problems Challenge 30
- **Prompt:** Solve the problem with clear constraints and edge-case handling.
- **Solution idea:** Start with a brute-force baseline, then optimize using the canonical pattern for this category.
- **Complexity target:** Improve from O(n^2) to O(n) or O(n log n) where possible.
- **Java hint:** Write a pure function, then add focused unit tests.

### Problem 479. Java Streams Problems Challenge 31
- **Prompt:** Solve the problem with clear constraints and edge-case handling.
- **Solution idea:** Start with a brute-force baseline, then optimize using the canonical pattern for this category.
- **Complexity target:** Improve from O(n^2) to O(n) or O(n log n) where possible.
- **Java hint:** Write a pure function, then add focused unit tests.

### Problem 480. Java Streams Problems Challenge 32
- **Prompt:** Solve the problem with clear constraints and edge-case handling.
- **Solution idea:** Start with a brute-force baseline, then optimize using the canonical pattern for this category.
- **Complexity target:** Improve from O(n^2) to O(n) or O(n log n) where possible.
- **Java hint:** Write a pure function, then add focused unit tests.

## Multithreading Problems

### Problem 481. Multithreading Problems Challenge 1
- **Prompt:** Solve the problem with clear constraints and edge-case handling.
- **Solution idea:** Start with a brute-force baseline, then optimize using the canonical pattern for this category.
- **Complexity target:** Improve from O(n^2) to O(n) or O(n log n) where possible.
- **Java hint:** Write a pure function, then add focused unit tests.

### Problem 482. Multithreading Problems Challenge 2
- **Prompt:** Solve the problem with clear constraints and edge-case handling.
- **Solution idea:** Start with a brute-force baseline, then optimize using the canonical pattern for this category.
- **Complexity target:** Improve from O(n^2) to O(n) or O(n log n) where possible.
- **Java hint:** Write a pure function, then add focused unit tests.

### Problem 483. Multithreading Problems Challenge 3
- **Prompt:** Solve the problem with clear constraints and edge-case handling.
- **Solution idea:** Start with a brute-force baseline, then optimize using the canonical pattern for this category.
- **Complexity target:** Improve from O(n^2) to O(n) or O(n log n) where possible.
- **Java hint:** Write a pure function, then add focused unit tests.

### Problem 484. Multithreading Problems Challenge 4
- **Prompt:** Solve the problem with clear constraints and edge-case handling.
- **Solution idea:** Start with a brute-force baseline, then optimize using the canonical pattern for this category.
- **Complexity target:** Improve from O(n^2) to O(n) or O(n log n) where possible.
- **Java hint:** Write a pure function, then add focused unit tests.

### Problem 485. Multithreading Problems Challenge 5
- **Prompt:** Solve the problem with clear constraints and edge-case handling.
- **Solution idea:** Start with a brute-force baseline, then optimize using the canonical pattern for this category.
- **Complexity target:** Improve from O(n^2) to O(n) or O(n log n) where possible.
- **Java hint:** Write a pure function, then add focused unit tests.

### Problem 486. Multithreading Problems Challenge 6
- **Prompt:** Solve the problem with clear constraints and edge-case handling.
- **Solution idea:** Start with a brute-force baseline, then optimize using the canonical pattern for this category.
- **Complexity target:** Improve from O(n^2) to O(n) or O(n log n) where possible.
- **Java hint:** Write a pure function, then add focused unit tests.

### Problem 487. Multithreading Problems Challenge 7
- **Prompt:** Solve the problem with clear constraints and edge-case handling.
- **Solution idea:** Start with a brute-force baseline, then optimize using the canonical pattern for this category.
- **Complexity target:** Improve from O(n^2) to O(n) or O(n log n) where possible.
- **Java hint:** Write a pure function, then add focused unit tests.

### Problem 488. Multithreading Problems Challenge 8
- **Prompt:** Solve the problem with clear constraints and edge-case handling.
- **Solution idea:** Start with a brute-force baseline, then optimize using the canonical pattern for this category.
- **Complexity target:** Improve from O(n^2) to O(n) or O(n log n) where possible.
- **Java hint:** Write a pure function, then add focused unit tests.

### Problem 489. Multithreading Problems Challenge 9
- **Prompt:** Solve the problem with clear constraints and edge-case handling.
- **Solution idea:** Start with a brute-force baseline, then optimize using the canonical pattern for this category.
- **Complexity target:** Improve from O(n^2) to O(n) or O(n log n) where possible.
- **Java hint:** Write a pure function, then add focused unit tests.

### Problem 490. Multithreading Problems Challenge 10
- **Prompt:** Solve the problem with clear constraints and edge-case handling.
- **Solution idea:** Start with a brute-force baseline, then optimize using the canonical pattern for this category.
- **Complexity target:** Improve from O(n^2) to O(n) or O(n log n) where possible.
- **Java hint:** Write a pure function, then add focused unit tests.

### Problem 491. Multithreading Problems Challenge 11
- **Prompt:** Solve the problem with clear constraints and edge-case handling.
- **Solution idea:** Start with a brute-force baseline, then optimize using the canonical pattern for this category.
- **Complexity target:** Improve from O(n^2) to O(n) or O(n log n) where possible.
- **Java hint:** Write a pure function, then add focused unit tests.

### Problem 492. Multithreading Problems Challenge 12
- **Prompt:** Solve the problem with clear constraints and edge-case handling.
- **Solution idea:** Start with a brute-force baseline, then optimize using the canonical pattern for this category.
- **Complexity target:** Improve from O(n^2) to O(n) or O(n log n) where possible.
- **Java hint:** Write a pure function, then add focused unit tests.

### Problem 493. Multithreading Problems Challenge 13
- **Prompt:** Solve the problem with clear constraints and edge-case handling.
- **Solution idea:** Start with a brute-force baseline, then optimize using the canonical pattern for this category.
- **Complexity target:** Improve from O(n^2) to O(n) or O(n log n) where possible.
- **Java hint:** Write a pure function, then add focused unit tests.

### Problem 494. Multithreading Problems Challenge 14
- **Prompt:** Solve the problem with clear constraints and edge-case handling.
- **Solution idea:** Start with a brute-force baseline, then optimize using the canonical pattern for this category.
- **Complexity target:** Improve from O(n^2) to O(n) or O(n log n) where possible.
- **Java hint:** Write a pure function, then add focused unit tests.

### Problem 495. Multithreading Problems Challenge 15
- **Prompt:** Solve the problem with clear constraints and edge-case handling.
- **Solution idea:** Start with a brute-force baseline, then optimize using the canonical pattern for this category.
- **Complexity target:** Improve from O(n^2) to O(n) or O(n log n) where possible.
- **Java hint:** Write a pure function, then add focused unit tests.

### Problem 496. Multithreading Problems Challenge 16
- **Prompt:** Solve the problem with clear constraints and edge-case handling.
- **Solution idea:** Start with a brute-force baseline, then optimize using the canonical pattern for this category.
- **Complexity target:** Improve from O(n^2) to O(n) or O(n log n) where possible.
- **Java hint:** Write a pure function, then add focused unit tests.

### Problem 497. Multithreading Problems Challenge 17
- **Prompt:** Solve the problem with clear constraints and edge-case handling.
- **Solution idea:** Start with a brute-force baseline, then optimize using the canonical pattern for this category.
- **Complexity target:** Improve from O(n^2) to O(n) or O(n log n) where possible.
- **Java hint:** Write a pure function, then add focused unit tests.

### Problem 498. Multithreading Problems Challenge 18
- **Prompt:** Solve the problem with clear constraints and edge-case handling.
- **Solution idea:** Start with a brute-force baseline, then optimize using the canonical pattern for this category.
- **Complexity target:** Improve from O(n^2) to O(n) or O(n log n) where possible.
- **Java hint:** Write a pure function, then add focused unit tests.

### Problem 499. Multithreading Problems Challenge 19
- **Prompt:** Solve the problem with clear constraints and edge-case handling.
- **Solution idea:** Start with a brute-force baseline, then optimize using the canonical pattern for this category.
- **Complexity target:** Improve from O(n^2) to O(n) or O(n log n) where possible.
- **Java hint:** Write a pure function, then add focused unit tests.

### Problem 500. Multithreading Problems Challenge 20
- **Prompt:** Solve the problem with clear constraints and edge-case handling.
- **Solution idea:** Start with a brute-force baseline, then optimize using the canonical pattern for this category.
- **Complexity target:** Improve from O(n^2) to O(n) or O(n log n) where possible.
- **Java hint:** Write a pure function, then add focused unit tests.

### Problem 501. Multithreading Problems Challenge 21
- **Prompt:** Solve the problem with clear constraints and edge-case handling.
- **Solution idea:** Start with a brute-force baseline, then optimize using the canonical pattern for this category.
- **Complexity target:** Improve from O(n^2) to O(n) or O(n log n) where possible.
- **Java hint:** Write a pure function, then add focused unit tests.

### Problem 502. Multithreading Problems Challenge 22
- **Prompt:** Solve the problem with clear constraints and edge-case handling.
- **Solution idea:** Start with a brute-force baseline, then optimize using the canonical pattern for this category.
- **Complexity target:** Improve from O(n^2) to O(n) or O(n log n) where possible.
- **Java hint:** Write a pure function, then add focused unit tests.

### Problem 503. Multithreading Problems Challenge 23
- **Prompt:** Solve the problem with clear constraints and edge-case handling.
- **Solution idea:** Start with a brute-force baseline, then optimize using the canonical pattern for this category.
- **Complexity target:** Improve from O(n^2) to O(n) or O(n log n) where possible.
- **Java hint:** Write a pure function, then add focused unit tests.

### Problem 504. Multithreading Problems Challenge 24
- **Prompt:** Solve the problem with clear constraints and edge-case handling.
- **Solution idea:** Start with a brute-force baseline, then optimize using the canonical pattern for this category.
- **Complexity target:** Improve from O(n^2) to O(n) or O(n log n) where possible.
- **Java hint:** Write a pure function, then add focused unit tests.

### Problem 505. Multithreading Problems Challenge 25
- **Prompt:** Solve the problem with clear constraints and edge-case handling.
- **Solution idea:** Start with a brute-force baseline, then optimize using the canonical pattern for this category.
- **Complexity target:** Improve from O(n^2) to O(n) or O(n log n) where possible.
- **Java hint:** Write a pure function, then add focused unit tests.

### Problem 506. Multithreading Problems Challenge 26
- **Prompt:** Solve the problem with clear constraints and edge-case handling.
- **Solution idea:** Start with a brute-force baseline, then optimize using the canonical pattern for this category.
- **Complexity target:** Improve from O(n^2) to O(n) or O(n log n) where possible.
- **Java hint:** Write a pure function, then add focused unit tests.

### Problem 507. Multithreading Problems Challenge 27
- **Prompt:** Solve the problem with clear constraints and edge-case handling.
- **Solution idea:** Start with a brute-force baseline, then optimize using the canonical pattern for this category.
- **Complexity target:** Improve from O(n^2) to O(n) or O(n log n) where possible.
- **Java hint:** Write a pure function, then add focused unit tests.

### Problem 508. Multithreading Problems Challenge 28
- **Prompt:** Solve the problem with clear constraints and edge-case handling.
- **Solution idea:** Start with a brute-force baseline, then optimize using the canonical pattern for this category.
- **Complexity target:** Improve from O(n^2) to O(n) or O(n log n) where possible.
- **Java hint:** Write a pure function, then add focused unit tests.

### Problem 509. Multithreading Problems Challenge 29
- **Prompt:** Solve the problem with clear constraints and edge-case handling.
- **Solution idea:** Start with a brute-force baseline, then optimize using the canonical pattern for this category.
- **Complexity target:** Improve from O(n^2) to O(n) or O(n log n) where possible.
- **Java hint:** Write a pure function, then add focused unit tests.

### Problem 510. Multithreading Problems Challenge 30
- **Prompt:** Solve the problem with clear constraints and edge-case handling.
- **Solution idea:** Start with a brute-force baseline, then optimize using the canonical pattern for this category.
- **Complexity target:** Improve from O(n^2) to O(n) or O(n log n) where possible.
- **Java hint:** Write a pure function, then add focused unit tests.

### Problem 511. Multithreading Problems Challenge 31
- **Prompt:** Solve the problem with clear constraints and edge-case handling.
- **Solution idea:** Start with a brute-force baseline, then optimize using the canonical pattern for this category.
- **Complexity target:** Improve from O(n^2) to O(n) or O(n log n) where possible.
- **Java hint:** Write a pure function, then add focused unit tests.

### Problem 512. Multithreading Problems Challenge 32
- **Prompt:** Solve the problem with clear constraints and edge-case handling.
- **Solution idea:** Start with a brute-force baseline, then optimize using the canonical pattern for this category.
- **Complexity target:** Improve from O(n^2) to O(n) or O(n log n) where possible.
- **Java hint:** Write a pure function, then add focused unit tests.
